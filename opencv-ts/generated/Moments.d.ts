// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

/**
 * struct returned by cv::moments
 *
 * The spatial moments  are computed as:
 *
 *
 *
 *
 *
 * The central moments  are computed as:
 *
 *
 *
 *
 *
 * where  is the mass center:
 *
 *
 *
 *
 *
 * The normalized central moments  are computed as:
 *
 *
 *
 *
 *
 * @note ,   , hence the values are not stored.
 *
 * The moments of a contour are defined in the same way but computed using the Green's formula (see http://en.wikipedia.org/wiki/Green_theorem (http://en.wikipedia.org/wiki/Green_theorem)). So, due to a limited raster resolution, the moments computed for a contour are slightly different from the moments computed for the same rasterized contour.
 *
 *
 * @note Since the contour moments are computed using Green formula, you may get seemingly odd results for contours with self-intersections, e.g. a zero area (m00) for butterfly-shaped contours.
 */
export declare class Moments {
/**
 * the default constructor
 */
  public constructor()

/**
 * the full constructor
 */
  public constructor(m00: number, m10: number, m01: number, m20: number, m11: number, m02: number, m30: number, m21: number, m12: number, m03: number)
}
