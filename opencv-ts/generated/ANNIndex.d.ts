// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray, OutputArray } from '../hacks/mat';
import type { Ptr } from './_unresolved';

export declare class ANNIndex {
/**
 * Add feature vectors to index.
 *
 * @param features Matrix containing the feature vectors to index. The size of the matrix is num_features x feature_dimension.
 */
  public addItems(features: InputArray): void

/**
 * Build the index.
 *
 * @param trees Number of trees in the index. If not provided, the number is determined automatically in a way that at most 2x as much memory as the features vectors take is used.
 */
  public build(trees?: number): void

/**
 * Return the number of feature vectors in the index.
 */
  public getItemNumber(): number

/**
 * Return the number of trees in the index.
 */
  public getTreeNumber(): number

/**
 * Performs a K-nearest neighbor search for given query vector(s) using the index.
 *
 * @param query The query vector(s).
 * @param indices Matrix that will contain the indices of the K-nearest neighbors found, optional.
 * @param dists Matrix that will contain the distances to the K-nearest neighbors found, optional.
 * @param knn Number of nearest neighbors to search for.
 * @param search_k The maximum number of nodes to inspect, which defaults to trees x knn if not provided.
 */
  public knnSearch(query: InputArray, indices: OutputArray, dists: OutputArray, knn: number, search_k?: number): void

/**
 * Loads (mmaps) an index from disk.
 *
 * @param filename Filename of the index to be loaded.
 * @param prefault If prefault is set to true, it will pre-read the entire file into memory (using mmap with MAP_POPULATE). Default is false.
 */
  public load(filename: String, prefault?: boolean): void

/**
 * Save the index to disk and loads it. After saving, no more vectors can be added.
 *
 * @param filename Filename of the index to be saved.
 * @param prefault If prefault is set to true, it will pre-read the entire file into memory (using mmap with MAP_POPULATE). Default is false.
 */
  public save(filename: String, prefault?: boolean): void

/**
 * Prepare to build the index in the specified file instead of RAM (execute before adding items, no need to save after build)
 *
 * @param filename Filename of the index to be built.
 */
  public setOnDiskBuild(filename: String): boolean

/**
 * Initialize the random number generator with the given seed. Only necessary to pass this before adding the items. Will have no effect after calling build() or load().
 *
 * @param seed The given seed of the random number generator. Its value should be within the range of uint32_t.
 */
  public setSeed(seed: number): void

/**
 * Creates an instance of annoy index class with given parameters.
 *
 * @param dim The dimension of the feature vector.
 * @param distType Metric to calculate the distance between two feature vectors, can be DIST_EUCLIDEAN, DIST_MANHATTAN, DIST_ANGULAR, DIST_HAMMING, or DIST_DOTPRODUCT.
 */
  public static create(dim: number, distType?: any): Ptr
}

export type ANNIndex_Distance = number

export declare const ANNIndex_DIST_EUCLIDEAN: ANNIndex_Distance

export declare const ANNIndex_DIST_MANHATTAN: ANNIndex_Distance

export declare const ANNIndex_DIST_ANGULAR: ANNIndex_Distance

export declare const ANNIndex_DIST_HAMMING: ANNIndex_Distance

export declare const ANNIndex_DIST_DOTPRODUCT: ANNIndex_Distance
