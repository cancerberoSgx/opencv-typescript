// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray, InputArrayOfArrays } from '../hacks/mat';
import type { Vector } from '../hacks/runtime';
import type { CV_WRAP_FILE_PATH } from './_unresolved';
import type { dnn_Model } from './dnn_Model';
import type { dnn_Net } from './dnn_Net';

/**
 * This class represents high-level API for text recognition networks.
 *
 * TextRecognitionModel allows to set params for preprocessing input image. TextRecognitionModel creates net from file with trained weights and config, sets preprocessing input, runs forward pass and return recognition result. For TextRecognitionModel, CRNN-CTC is supported.
 */
export declare class dnn_TextRecognitionModel extends dnn_Model {
  public constructor()

/**
 * Create Text Recognition model from deep learning network Call setDecodeType() and setVocabulary() after constructor to initialize the decoding method.
 *
 * @param network Net object
 */
  public constructor(network: dnn_Net)

/**
 * Create text recognition model from network represented in one of the supported formats Call setDecodeType() and setVocabulary() after constructor to initialize the decoding method.
 *
 * @param model Binary file contains trained weights
 * @param config Text file contains network configuration
 */
  public constructor(model: CV_WRAP_FILE_PATH, config?: CV_WRAP_FILE_PATH)

/**
 * Get the decoding method.
 *
 * @return the decoding method
 */
  public getDecodeType(): any

/**
 * Get the vocabulary for recognition.
 *
 * @return vocabulary the associated vocabulary
 */
  public getVocabulary(): any

/**
 * Given the `input` frame, create input blob, run net and return recognition result.
 *
 * @return The text recognition result
 *
 * @param frame The input image
 */
  public recognize(frame: InputArray): any

/**
 * Given the `input` frame, create input blob, run net and return recognition result.
 *
 * @param frame The input image
 * @param roiRects List of text detection regions of interest (cv::Rect, CV_32SC4). ROIs is be cropped as the network inputs
 * @param results A set of text recognition results.
 */
  public recognize(frame: InputArray, roiRects: InputArrayOfArrays, results: Vector<any>): void

/**
 * Set the decoding method options for `"CTC-prefix-beam-search"` decode usage.
 *
 * @param beamSize Beam size for search
 * @param vocPruneSize Parameter to optimize big vocabulary search, only take top `vocPruneSize` tokens in each search step, `vocPruneSize` <= 0 stands for disable this prune.
 */
  public setDecodeOptsCTCPrefixBeamSearch(beamSize: number, vocPruneSize?: number): dnn_TextRecognitionModel

/**
 * Set the decoding method of translating the network output into string.
 *
 * @param decodeType The decoding method of translating the network output into string, currently supported type: - `"CTC-greedy"` greedy decoding for the output of CTC-based methods - `"CTC-prefix-beam-search"` Prefix beam search decoding for the output of CTC-based methods
 */
  public setDecodeType(decodeType: any): dnn_TextRecognitionModel

/**
 * Set the vocabulary for recognition.
 *
 * @param vocabulary the associated vocabulary of the network.
 */
  public setVocabulary(vocabulary: any): dnn_TextRecognitionModel
}
