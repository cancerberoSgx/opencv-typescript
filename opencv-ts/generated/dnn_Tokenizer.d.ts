// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { Vector } from '../hacks/runtime';
import type { CV_WRAP_FILE_PATH } from './_unresolved';

/**
 * High-level tokenizer wrapper for DNN usage.
 *
 * Provides a simple API to encode and decode tokens for LLMs. Models are loaded via Tokenizer::load().
 *
 *
 * usingnamespacecv::dnn;
 * Tokenizertok=Tokenizer::load("/path/to/model/");
 * std::vector<int>ids=tok.encode("helloworld");
 * std::stringtext=tok.decode(ids);
 */
export declare class dnn_Tokenizer {
/**
 * Construct a tokenizer with a given method default BPE. For BPE method you normally call Tokenizer::load() to initialize model data.
 */
  public constructor()

  public decode(tokens: any): any

/**
 * Encode UTF-8 text to token ids (special tokens currently disabled).
 *
 * Calls the underlying `CoreBPE::encode` with an empty allowed-special set.
 *
 *
 * @return Vector of token ids (32-bit ids narrowed to int for convenience).
 *
 * @param text UTF-8 input string.
 */
  public encode(text: any): Vector<any>

/**
 * Load a tokenizer from a model directory.
 *
 * Expects the directory to contain:
 * - `config.json` with field `model_type` with value "gpt2" or "gpt4".
 * - `tokenizer.json` produced by the corresponding model family.
 *
 *
 * The argument is a path prefix; this function concatenates file names directly (e.g. `model_dir` + "config.json"), so `model_dir` must end with an appropriate path separator.
 *
 *
 * @return A Tokenizer ready for use. Throws cv::Exception if files are missing or `model_type` is unsupported.
 *
 * @param model_config Path to config.json for model.
 */
  public static load(model_config: CV_WRAP_FILE_PATH): dnn_Tokenizer
}
