// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { Size } from '../hacks/scalars';
import type { Feature2D } from './Feature2D';
import type { Ptr } from './_unresolved';

/**
 * XFeat feature detector and descriptor, based on a DNN model.
 *
 * XFeat is a compact learned local-feature extractor. This class wraps an ONNX export through cv::dnn::Net and exposes score-map detections with 64-D float descriptors under the standard cv::Feature2D interface.
 *
 *
 * The class assumes the ONNX model has a single grayscale input tensor N×1×H×W in [0, 1] and returns descriptor and score maps. The descriptor map must have 64 channels, and the keypoint logit map must have either 64 channels (no extra class) or 65 channels, where the last channel is treated as a dustbin/background class used only in softmax normalization. Images are resized with preserved aspect ratio and padded to the configured network input size.
 */
export declare class XFeat extends Feature2D {
/**
 * Returns the algorithm string identifier. This string is used as top level xml/yml node tag when the object is saved to a file or string.
 */
  public getDefaultName(): String

  public getInputSize(): Size

  public getMaxKeypoints(): number

  public getScoreThreshold(): number

  public setInputSize(inputSize: Size): void

  public setMaxKeypoints(maxKeypoints: number): void

  public setScoreThreshold(threshold: number): void

/**
 * Creates an XFeat detector from an in-memory model buffer.
 */
  public static create(bufferModel: number, maxKeypoints?: number, scoreThreshold?: number, inputSize?: Size, backendId?: number, targetId?: number): Ptr

/**
 * Creates an XFeat detector.
 *
 * @param modelPath Path to the XFeat ONNX model.
 * @param maxKeypoints Maximum number of keypoints to return per image. The strongest responses are kept; -1 keeps all detections.
 * @param scoreThreshold Discard keypoints with network score not greater than this value.
 * @param inputSize Input size fed to the network, default Size(640, 640).
 * @param backendId DNN backend identifier (see cv::dnn::Backend); 0 = DNN_BACKEND_DEFAULT.
 * @param targetId DNN target identifier (see cv::dnn::Target); 0 = DNN_TARGET_CPU.
 */
  public static create(modelPath: String, maxKeypoints?: number, scoreThreshold?: number, inputSize?: Size, backendId?: number, targetId?: number): Ptr
}
