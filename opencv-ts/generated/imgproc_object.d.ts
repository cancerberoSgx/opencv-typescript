// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray, OutputArray } from '../hacks/mat';
import type { Detection } from './_unresolved';

/* @defgroup Object Detection */

/**
 * Compares a template against overlapped image regions.
 *
 * The function slides through image , compares the overlapped patches of size  against templ using the specified method and stores the comparison results in result . TemplateMatchModes describes the formulae for the available comparison methods (  denotes image,  template,  result,  the optional mask ). The summation is done over template and/or the image patch:
 *
 *
 * After the function finishes the comparison, the best matches can be found as global minimums (when TM_SQDIFF was used) or maximums (when TM_CCORR or TM_CCOEFF was used) using the minMaxLoc function. In case of a color image, template summation in the numerator and each sum in the denominator is done over all of the channels and separate mean values are used for each channel. That is, the function can take a color template and a color image. The result will still be a single-channel image, which is easier to analyze.
 *
 * @param image Image where the search is running. It must be 8-bit or 32-bit floating-point.
 * @param templ Searched template. It must be not greater than the source image and have the same data type.
 * @param result Map of comparison results. It must be single-channel 32-bit floating-point. If image is  and templ is  , then result is  .
 * @param method Parameter specifying the comparison method, see TemplateMatchModes
 * @param mask Optional mask. It must have the same size as templ. It must either have the same number of channels as template or only one channel, which is then used for all template and image channels. If the data type is CV_8U, the mask is interpreted as a binary mask, meaning only elements where mask is nonzero are used and are kept unchanged independent of the actual mask value (weight equals 1). For data type CV_32F, the mask values are used as weights. The exact formulas are documented in TemplateMatchModes.
 */
export declare function matchTemplate(image: InputArray, templ: InputArray, result: OutputArray, method: number, mask?: InputArray): void

/**
 * with mask:
 */
export declare const TM_SQDIFF: number

/**
 * with mask:
 */
export declare const TM_SQDIFF_NORMED: number

/**
 * with mask:
 */
export declare const TM_CCORR: number

/**
 * with mask:
 */
export declare const TM_CCORR_NORMED: number

/**
 * where   with mask:
 */
export declare const TM_CCOEFF: number

export declare const TM_CCOEFF_NORMED: number
