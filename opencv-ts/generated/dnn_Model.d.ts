// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray, OutputArrayOfArrays } from '../hacks/mat';
import type { Scalar, Size } from '../hacks/scalars';
import type { CV_WRAP_FILE_PATH } from './_unresolved';
import type { dnn_Net } from './dnn_Net';

/**
 * This class is presented high-level API for neural networks.
 *
 * Model allows to set params for preprocessing input image. Model creates net from file with trained weights and config, sets preprocessing input and runs forward pass.
 */
export declare class dnn_Model {
  public constructor()

  public constructor(arg0: dnn_Model)

/**
 * Create model from deep learning network.
 *
 * @param network Net object.
 */
  public constructor(network: dnn_Net)

/**
 * Create model from deep learning network represented in one of the supported formats. An order of `model` and `config` arguments does not matter.
 *
 * @param model Binary file contains trained weights.
 * @param config Text file contains network configuration.
 */
  public constructor(model: CV_WRAP_FILE_PATH, config?: CV_WRAP_FILE_PATH)

  public constructor(arg0: dnn_Model)

/**
 * @see Net::enableWinograd
 */
  public enableWinograd(useWinograd: boolean): dnn_Model

  public getImpl(): any

  public getImplRef(): any

  public getNetwork_(): dnn_Net

  public getNetwork_(): dnn_Net

/**
 * Given the `input` frame, create input blob, run net and return the output `blobs`.
 *
 * @param frame The input image.
 * @param outs Allocated output blobs, which will store results of the computation.
 */
  public predict(frame: InputArray, outs: OutputArrayOfArrays): void

/**
 * Set flag crop for frame.
 *
 * @param crop Flag which indicates whether image will be cropped after resize or not.
 */
  public setInputCrop(crop: boolean): dnn_Model

/**
 * Set mean value for frame.
 *
 * @param mean Scalar with mean values which are subtracted from channels.
 */
  public setInputMean(mean: Scalar): dnn_Model

/**
 * Set preprocessing parameters for frame.
 *
 * @param scale Multiplier for frame values.
 * @param size New input size.
 * @param mean Scalar with mean values which are subtracted from channels.
 * @param swapRB Flag which indicates that swap first and last channels.
 * @param crop Flag which indicates whether image will be cropped after resize or not. blob(n, c, y, x) = scale * resize( frame(y, x, c) ) - mean(c) )
 */
  public setInputParams(scale?: number, size?: Size, mean?: Scalar, swapRB?: boolean, crop?: boolean): void

/**
 * Set scalefactor value for frame.
 *
 * @param scale Multiplier for frame values.
 */
  public setInputScale(scale: Scalar): dnn_Model

/**
 * Set input size for frame.
 *
 * @note If shape of the new blob less than 0, then frame size not change.
 *
 * @param size New input size.
 */
  public setInputSize(size: Size): dnn_Model

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param width New input width.
 * @param height New input height.
 */
  public setInputSize(width: number, height: number): dnn_Model

/**
 * Set flag swapRB for frame.
 *
 * @param swapRB Flag which indicates that swap first and last channels.
 */
  public setInputSwapRB(swapRB: boolean): dnn_Model

/**
 * Set output names for frame.
 *
 * @param outNames Names for output layers.
 */
  public setOutputNames(outNames: String): dnn_Model

/**
 * @see Net::setPreferableBackend
 */
  public setPreferableBackend(backendId: any): dnn_Model

/**
 * @see Net::setPreferableTarget
 */
  public setPreferableTarget(targetId: any): dnn_Model
}
