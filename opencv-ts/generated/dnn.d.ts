// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray, OutputArray } from '../hacks/mat';
import type { Mat } from './Mat';
import type { CV_WRAP_FILE_PATH, Net } from './_unresolved';

/**
 * This module contains:
 * - API for new layers creation, layers are building bricks of neural networks;
 * - set of built-in most-useful Layers;
 * - API to construct and modify comprehensive neural networks from layers;
 * - functionality for loading serialized networks models from different frameworks.
 *
 *
 * Functionality of this module is designed only for forward pass computations (i.e. network testing). A network training is in principle not supported.
 *
 *
 * HAL replacement hooks for the compute kernels of the DNN engine.
 *
 *
 * An accelerated backend (e.g. the RISC-V RVV HAL) overrides a kernel by redefining the matching `cv_hal_dnn_*` macro to point at its own function; the default `hal_ni_*` below returns `CV_HAL_ERROR_NOT_IMPLEMENTED`, so the built-in implementation runs unchanged when no backend is present.
 *
 *
 * Tensors use the engine's blocked NCDHWc layout described by `cv::dnn::ConvState`: `N` * C1 channel-blocks of `C0` channels each, laid out as [N, C1, ...spatial..., C0].
 *
 *
 * **Parallelism is owned by OpenCV, not by the HAL.** Each hook receives a task range ``[task_start, task_end) over the ``[0, N*C1) block-plane index and must compute *only* that slice of the output tensor. The engine drives `parallel_for_` and calls the hook once per worker range, so a hook must never spawn its own threads.
 */

/**
 * Creates 4-dimensional blob from image. Optionally resizes and crops `image` from center, subtract `mean` values, scales values by `scalefactor`, swap Blue and Red channels.
 *
 * if `crop` is true, input image is resized so one side after resize is equal to corresponding dimension in `size` and another one is equal or larger. Then, crop from the center is performed. If `crop` is false, direct resize without cropping and preserving aspect ratio is performed. @return 4-dimensional Mat with NCHW dimensions order.
 *
 * @note The order and usage of `scalefactor` and `mean` are (input - mean) * scalefactor.
 *
 * @param image input image (with 1-, 3- or 4-channels).
 * @param scalefactor multiplier for `images` values.
 * @param size spatial size for output image
 * @param mean scalar with mean values which are subtracted from channels. Values are intended to be in (mean-R, mean-G, mean-B) order if `image` has BGR ordering and `swapRB` is true.
 * @param swapRB flag which indicates that swap first and last channels in 3-channel image is necessary.
 * @param crop flag which indicates whether image will be cropped after resize or not
 * @param ddepth Depth of output blob. Choose CV_32F or CV_8U.
 */
export declare function blobFromImage(image: InputArray, scalefactor?: number, size?: any, mean?: any, swapRB?: boolean, crop?: boolean, ddepth?: number): Mat

/**
 * Creates 4-dimensional blob from image.
 *
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
export declare function blobFromImage(image: InputArray, blob: OutputArray, scalefactor?: number, size?: any, mean?: any, swapRB?: boolean, crop?: boolean, ddepth?: number): void

/**
 * Read deep learning network represented in one of the supported formats.
 *
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 * @return Net object.
 *
 * @param framework Name of origin framework.
 * @param bufferModel A buffer with a content of binary file with weights
 * @param bufferConfig A buffer with a content of text file contains network configuration.
 * @param engine select DNN engine to be used. ENGINE_AUTO (the default) resolves to ENGINE_OPENCV; ENGINE_ORT selects the ONNX Runtime wrapper (ONNX models only, requires WITH_ONNXRUNTIME=ON). Please pay attention that the new DNN does not support non-CPU back-ends for now.
 */
export declare function readNet(framework: any, bufferModel: number, bufferConfig?: number, engine?: number): Net

/**
 * Read deep learning network represented in one of the supported formats.
 *
 * @return Net object.
 *
 * This function automatically detects an origin framework of trained model and calls an appropriate function such readNetFromTensorflow, readNetFromONNX. An order of `model` and `config` arguments does not matter.
 *
 * @param model Binary file contains trained weights. The following file extensions are expected for models from different frameworks: - `*.pb` (TensorFlow, https://www.tensorflow.org/ (https://www.tensorflow.org/)) - `*.bin` | `*.onnx` (OpenVINO, https://software.intel.com/openvino-toolkit (https://software.intel.com/openvino-toolkit)) - `*.onnx` (ONNX, https://onnx.ai/ (https://onnx.ai/))
 * @param config Text file contains network configuration. It could be a file with the following extensions: - `*.pbtxt` (TensorFlow, https://www.tensorflow.org/ (https://www.tensorflow.org/)) - `*.xml` (OpenVINO, https://software.intel.com/openvino-toolkit (https://software.intel.com/openvino-toolkit))
 * @param framework Explicit framework name tag to determine a format.
 * @param engine select DNN engine to be used. ENGINE_AUTO (the default) resolves to ENGINE_OPENCV; ENGINE_ORT selects the ONNX Runtime wrapper (ONNX models only, requires WITH_ONNXRUNTIME=ON). Please pay attention that the new DNN does not support non-CPU back-ends for now.
 */
export declare function readNet(model: CV_WRAP_FILE_PATH, config?: CV_WRAP_FILE_PATH, framework?: any, engine?: number): Net

/**
 * Reads a network model from ONNX (https://onnx.ai/) in-memory buffer.
 *
 * @return Network object that ready to do forward, throw an exception in failure cases.
 *
 * @param buffer memory address of the first byte of the buffer.
 * @param sizeBuffer size of the buffer.
 * @param engine select DNN engine to be used. ENGINE_AUTO (the default) resolves to ENGINE_OPENCV; ENGINE_ORT selects the ONNX Runtime wrapper (ONNX models only, requires WITH_ONNXRUNTIME=ON).
 */
export declare function readNetFromONNX(buffer: any, sizeBuffer: number, engine?: number): Net

/**
 * Reads a network model from ONNX (https://onnx.ai/) in-memory buffer.
 *
 * @return Network object that ready to do forward, throw an exception in failure cases.
 *
 * @param buffer in-memory buffer that stores the ONNX model bytes.
 * @param engine select DNN engine to be used. ENGINE_AUTO (the default) resolves to ENGINE_OPENCV; ENGINE_ORT selects the ONNX Runtime wrapper (ONNX models only, requires WITH_ONNXRUNTIME=ON). Please pay attention that the new DNN does not support non-CPU back-ends for now.
 */
export declare function readNetFromONNX(buffer: number, engine?: number): Net

/**
 * Reads a network model ONNX (https://onnx.ai/).
 *
 * @return Network object that ready to do forward, throw an exception in failure cases.
 *
 * @param onnxFile path to the .onnx file with text description of the network architecture.
 * @param engine select DNN engine to be used. ENGINE_AUTO (the default) resolves to ENGINE_OPENCV; ENGINE_ORT selects the ONNX Runtime wrapper (ONNX models only, requires WITH_ONNXRUNTIME=ON). Please pay attention that the new DNN does not support non-CPU back-ends for now.
 */
export declare function readNetFromONNX(onnxFile: CV_WRAP_FILE_PATH, engine?: number): Net

/**
 * Reads a network model stored in TensorFlow (https://www.tensorflow.org/) framework's format.
 *
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param bufferModel buffer containing the content of the pb file
 * @param lenModel length of bufferModel
 * @param bufferConfig buffer containing the content of the pbtxt file
 * @param lenConfig length of bufferConfig
 * @param engine select DNN engine to be used. ENGINE_AUTO (the default) resolves to ENGINE_OPENCV.
 * @param extraOutputs specify model outputs explicitly, in addition to the outputs the graph analyzer finds. Please pay attention that the new DNN does not support non-CPU back-ends for now.
 */
export declare function readNetFromTensorflow(bufferModel: any, lenModel: number, bufferConfig?: any, lenConfig?: number, engine?: number, extraOutputs?: any): Net

/**
 * Reads a network model stored in TensorFlow (https://www.tensorflow.org/) framework's format.
 *
 * @return Net object.
 *
 * @param bufferModel buffer containing the content of the pb file
 * @param bufferConfig buffer containing the content of the pbtxt file
 * @param engine select DNN engine to be used. ENGINE_AUTO (the default) resolves to ENGINE_OPENCV.
 * @param extraOutputs specify model outputs explicitly, in addition to the outputs the graph analyzer finds. Please pay attention that the new DNN does not support non-CPU back-ends for now.
 */
export declare function readNetFromTensorflow(bufferModel: number, bufferConfig?: number, engine?: number, extraOutputs?: any): Net

/**
 * Reads a network model stored in TensorFlow (https://www.tensorflow.org/) framework's format.
 *
 * @return Net object.
 *
 * @param model path to the .pb file with binary protobuf description of the network architecture
 * @param config path to the .pbtxt file that contains text graph definition in protobuf format. Resulting Net object is built by text graph using weights from a binary one that let us make it more flexible.
 * @param engine select DNN engine to be used. ENGINE_AUTO (the default) resolves to ENGINE_OPENCV.
 * @param extraOutputs specify model outputs explicitly, in addition to the outputs the graph analyzer finds. Please pay attention that the new DNN does not support non-CPU back-ends for now.
 */
export declare function readNetFromTensorflow(model: CV_WRAP_FILE_PATH, config?: CV_WRAP_FILE_PATH, engine?: number, extraOutputs?: any): Net

/**
 * Reads a network model stored in TFLite (https://www.tensorflow.org/lite) framework's format.
 *
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param bufferModel buffer containing the content of the tflite file
 * @param lenModel length of bufferModel
 * @param engine select DNN engine to be used. ENGINE_AUTO (the default) resolves to ENGINE_OPENCV; ENGINE_ORT selects the ONNX Runtime wrapper (ONNX models only, requires WITH_ONNXRUNTIME=ON). Please pay attention that the new DNN does not support non-CPU back-ends for now.
 */
export declare function readNetFromTFLite(bufferModel: any, lenModel: number, engine?: number): Net

/**
 * Reads a network model stored in TFLite (https://www.tensorflow.org/lite) framework's format.
 *
 * @return Net object.
 *
 * @param bufferModel buffer containing the content of the tflite file
 * @param engine select DNN engine to be used. ENGINE_AUTO (the default) resolves to ENGINE_OPENCV; ENGINE_ORT selects the ONNX Runtime wrapper (ONNX models only, requires WITH_ONNXRUNTIME=ON). Please pay attention that the new DNN does not support non-CPU back-ends for now.
 */
export declare function readNetFromTFLite(bufferModel: number, engine?: number): Net

/**
 * Reads a network model stored in TFLite (https://www.tensorflow.org/lite) framework's format.
 *
 * @return Net object.
 *
 * @param model path to the .tflite file with binary flatbuffers description of the network architecture
 * @param engine select DNN engine to be used. ENGINE_AUTO (the default) resolves to ENGINE_OPENCV; ENGINE_ORT selects the ONNX Runtime wrapper (ONNX models only, requires WITH_ONNXRUNTIME=ON). Please pay attention that the new DNN does not support non-CPU back-ends for now.
 */
export declare function readNetFromTFLite(model: CV_WRAP_FILE_PATH, engine?: number): Net

/**
 * valid only for Arg.idx==0. It's "no-arg"
 */
export declare const DNN_ARG_EMPTY: number

/**
 * a constant argument.
 */
export declare const DNN_ARG_CONST: number

/**
 * input of the whole model. Before Net::forward() or in Net::forward() all inputs must be set
 */
export declare const DNN_ARG_INPUT: number

/**
 * output of the model.
 */
export declare const DNN_ARG_OUTPUT: number

/**
 * intermediate result, a result of some operation and input to some other operation(s).
 */
export declare const DNN_ARG_TEMP: number

/**
 * not used for now
 */
export declare const DNN_ARG_PATTERN: number

/**
 * DNN_BACKEND_DEFAULT equals to OPENCV_DNN_BACKEND_DEFAULT, which can be defined using CMake or a configuration parameter.
 */
export declare const DNN_BACKEND_DEFAULT: number

/**
 * Intel OpenVINO computational backend, supported targets: CPU, OPENCL, OPENCL_FP16, MYRIAD, HDDL, NPU @note Tutorial how to build OpenCV with OpenVINO: OpenCV usage with OpenVINO
 */
export declare const DNN_BACKEND_INFERENCE_ENGINE: number

export declare const DNN_BACKEND_OPENCV: number

export declare const DNN_BACKEND_VKCOM: number

export declare const DNN_BACKEND_CUDA: number

export declare const DNN_BACKEND_WEBNN: number

export declare const DNN_BACKEND_TIMVX: number

export declare const DNN_BACKEND_CANN: number

/**
 * Automatically select the engine. Currently resolves to ENGINE_OPENCV; the mapping may change as more engines are added.
 */
export declare const ENGINE_AUTO: number

/**
 * Use OpenCV's built-in DNN engine. Does not support non-CPU back-ends for now.
 */
export declare const ENGINE_OPENCV: number

/**
 * Use the ONNX Runtime wrapper (ONNX only, requires build with WITH_ONNXRUNTIME=ON).
 */
export declare const ENGINE_ORT: number

export declare const DNN_PMODE_NULL: number

export declare const DNN_PMODE_CROP_CENTER: number

export declare const DNN_PMODE_LETTERBOX: number

/**
 * Some generic model format.
 */
export declare const DNN_MODEL_GENERIC: number

/**
 * ONNX model.
 */
export declare const DNN_MODEL_ONNX: number

/**
 * TF model.
 */
export declare const DNN_MODEL_TF: number

/**
 * TFLite model.
 */
export declare const DNN_MODEL_TFLITE: number

/**
 * Don't do any profiling.
 */
export declare const DNN_PROFILE_NONE: number

/**
 * Collect the summary statistics by layer type (e.g. all "Conv2D" or all "Add") and print it in the end, sorted by the execution time (most expensive layers first). Note that it may introduce some overhead and cause slowdown, especially in the case of non-CPU backends.
 */
export declare const DNN_PROFILE_SUMMARY: number

/**
 * Print execution time of each single layer. Note that it may introduce some overhead and cause slowdown, especially in the case of non-CPU backends.
 */
export declare const DNN_PROFILE_DETAILED: number

export declare const DNN_TARGET_CPU: number

export declare const DNN_TARGET_OPENCL: number

export declare const DNN_TARGET_OPENCL_FP16: number

export declare const DNN_TARGET_MYRIAD: number

export declare const DNN_TARGET_VULKAN: number

/**
 * FPGA device with CPU fallbacks using Inference Engine's Heterogeneous plugin.
 */
export declare const DNN_TARGET_FPGA: number

export declare const DNN_TARGET_CUDA: number

export declare const DNN_TARGET_CUDA_FP16: number

export declare const DNN_TARGET_HDDL: number

export declare const DNN_TARGET_NPU: number

export declare const DNN_TARGET_CPU_FP16: number

/**
 * Don't trace anything.
 */
export declare const DNN_TRACE_NONE: number

/**
 * Print all executed operations along with the output tensors, more or less compatible with ONNX Runtime.
 */
export declare const DNN_TRACE_ALL: number

/**
 * Print all executed operations. Types and shapes of all inputs and outputs are printed, but the content is not.
 */
export declare const DNN_TRACE_OP: number
