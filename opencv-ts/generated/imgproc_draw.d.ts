// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray, InputArrayOfArrays, InputOutputArray } from '../hacks/mat';
import type { Vector } from '../hacks/runtime';
import type { Point, Point2d, Range, Rect, Scalar, Size, Size2d, Size2l } from '../hacks/scalars';
import type { PutTextFlags } from './_unresolved';

/**
 * Drawing functions work with matrices/images of arbitrary depth. The boundaries of the shapes can be rendered with antialiasing (implemented only for 8-bit images for now). All the functions include the parameter color that uses an RGB value (that may be constructed with the Scalar constructor ) for color images and brightness for grayscale images. For color images, the channel ordering is normally *Blue, Green, Red*. This is what imshow, imread, and imwrite expect. So, if you form a color using the Scalar constructor, it should look like:
 *
 *
 *
 *
 *
 * If you are using your own image rendering and I/O functions, you can use any channel ordering. The drawing functions process each channel independently and do not depend on the channel order or even on the used color space. The whole image can be converted from BGR to RGB or to a different color space using cvtColor .
 *
 *
 * If a drawn figure is partially or completely outside the image, the drawing functions clip it. Also, many drawing functions can handle pixel coordinates specified with sub-pixel accuracy. This means that the coordinates can be passed as fixed-point numbers encoded as integers. The number of fractional bits is specified by the shift parameter and the real point coordinates are calculated as  . This feature is especially effective when rendering antialiased shapes.
 *
 *
 * @note The functions do not support alpha-transparency when the target image is 4-channel. In this case, the color[3] is simply copied to the repainted pixels. Thus, if you want to paint semi-transparent shapes, you can paint them in a separate buffer and then blend it with the main image.
 */

/**
 * Draws an arrow segment pointing from the first point to the second one.
 *
 * The function cv::arrowedLine draws an arrow between pt1 and pt2 points in the image. See also line.
 *
 * @param img Image.
 * @param pt1 The point the arrow starts from.
 * @param pt2 The point the arrow points to.
 * @param color Line color.
 * @param thickness Line thickness.
 * @param line_type Type of the line. See LineTypes
 * @param shift Number of fractional bits in the point coordinates.
 * @param tipLength The length of the arrow tip in relation to the arrow length
 */
export declare function arrowedLine(img: InputOutputArray, pt1: Point, pt2: Point, color: any, thickness?: number, line_type?: number, shift?: number, tipLength?: number): void

/**
 * Draws a circle.
 *
 * The function cv::circle draws a simple or filled circle with a given center and radius.
 *
 * @param img Image where the circle is drawn.
 * @param center Center of the circle.
 * @param radius Radius of the circle.
 * @param color Circle color.
 * @param thickness Thickness of the circle outline, if positive. Negative values, like FILLED, mean that a filled circle is to be drawn.
 * @param lineType Type of the circle boundary. See LineTypes
 * @param shift Number of fractional bits in the coordinates of the center and in the radius value.
 */
export declare function circle(img: InputOutputArray, center: Point, radius: number, color: any, thickness?: number, lineType?: number, shift?: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param imgRect Image rectangle.
 * @param pt1 First line point.
 * @param pt2 Second line point.
 */
export declare function clipLine(imgRect: Rect, pt1: any, pt2: any): boolean

/**
 * Clips the line against the image rectangle.
 *
 * The function cv::clipLine calculates a part of the line segment that is entirely within the specified rectangle. It returns false if the line segment is completely outside the rectangle. Otherwise, it returns true .
 *
 * @param imgSize Image size. The image rectangle is Rect(0, 0, imgSize.width, imgSize.height) .
 * @param pt1 First line point.
 * @param pt2 Second line point.
 */
export declare function clipLine(imgSize: Size, pt1: any, pt2: any): boolean

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param imgSize Image size. The image rectangle is Rect(0, 0, imgSize.width, imgSize.height) .
 * @param pt1 First line point.
 * @param pt2 Second line point.
 */
export declare function clipLine(imgSize: Size2l, pt1: any, pt2: any): boolean

/**
 * Draws contours outlines or filled contours.
 *
 * The function draws contour outlines in the image if  or fills the area bounded by the contours if  . The example below shows how to retrieve connected components from the binary image and label them: : #include"opencv2/imgproc.hpp"
 * #include"opencv2/highgui.hpp"
 *
 * usingnamespacecv;
 * usingnamespacestd;
 *
 * intmain(intargc,char**argv)
 * {
 * Matsrc;
 * //thefirstcommand-lineparametermustbeafilenameofthebinary
 * //(black-n-white)image
 * if(argc!=2||!(src=imread(argv[1],IMREAD_GRAYSCALE)).data)
 * return-1;
 *
 * Matdst=Mat::zeros(src.rows,src.cols,CV_8UC3);
 *
 * src=src>1;
 * namedWindow("Source",1);
 * imshow("Source",src);
 *
 * vector<vector<Point>>contours;
 * vector<Vec4i>hierarchy;
 *
 * findContours(src,contours,hierarchy,
 * RETR_CCOMP,CHAIN_APPROX_SIMPLE);
 *
 * //iteratethroughallthetop-levelcontours,
 * //draweachconnectedcomponentwithitsownrandomcolor
 * intidx=0;
 * for(;idx>=0;idx=hierarchy[idx][0])
 * {
 * Scalarcolor(rand()&255,rand()&255,rand()&255);
 * drawContours(dst,contours,idx,color,FILLED,8,hierarchy);
 * }
 *
 * namedWindow("Components",1);
 * imshow("Components",dst);
 * waitKey(0);
 * }
 *
 *
 * @note When thickness=FILLED, the function is designed to handle connected components with holes correctly even when no hierarchy data is provided. This is done by analyzing all the outlines together using even-odd rule. This may give incorrect results if you have a joint collection of separately retrieved contours. In order to solve this problem, you need to call drawContours separately for each sub-group of contours, or iterate over the collection using contourIdx parameter.
 *
 * @param image Destination image.
 * @param contours All the input contours. Each contour is stored as a point vector.
 * @param contourIdx Parameter indicating a contour to draw. If it is negative, all the contours are drawn.
 * @param color Color of the contours.
 * @param thickness Thickness of lines the contours are drawn with. If it is negative (for example, thickness=FILLED ), the contour interiors are drawn.
 * @param lineType Line connectivity. See LineTypes
 * @param hierarchy Optional information about hierarchy. It is only needed if you want to draw only some of the contours (see maxLevel ).
 * @param maxLevel Maximal level for drawn contours. If it is 0, only the specified contour is drawn. If it is 1, the function draws the contour(s) and all the nested contours. If it is 2, the function draws the contours, all the nested contours, all the nested-to-nested contours, and so on. This parameter is only taken into account when there is hierarchy available.
 * @param offset Optional contour shift parameter. Shift all the drawn contours by the specified  .
 */
export declare function drawContours(image: InputOutputArray, contours: InputArrayOfArrays, contourIdx: number, color: any, thickness?: number, lineType?: number, hierarchy?: InputArray, maxLevel?: number, offset?: Point): void

/**
 * Draw axes of the world/object coordinate system from pose estimation.
 *
 * @see solvePnP
 *
 *
 * This function draws the axes of the world/object coordinate system w.r.t. to the camera frame. OX is drawn in red, OY in green and OZ in blue.
 *
 * @param image Input/output image. It must have 1 or 3 channels. The number of channels is not altered.
 * @param cameraMatrix Input 3x3 floating-point matrix of camera intrinsic parameters.
 * @param distCoeffs Input vector of distortion coefficients . If the vector is empty, the zero distortion coefficients are assumed.
 * @param rvec Rotation vector (see Rodrigues ) that, together with tvec, brings points from the model coordinate system to the camera coordinate system.
 * @param tvec Translation vector.
 * @param length Length of the painted axes in the same unit than tvec (usually in meters).
 * @param thickness Line thickness of the painted axes.
 */
export declare function drawFrameAxes(image: InputOutputArray, cameraMatrix: InputArray, distCoeffs: InputArray, rvec: InputArray, tvec: InputArray, length: number, thickness?: number): void

/**
 * Draws a marker on a predefined position in an image.
 *
 * The function cv::drawMarker draws a marker on a given position in the image. For the moment several marker types are supported, see MarkerTypes for more information.
 *
 * @param img Image.
 * @param position The point where the crosshair is positioned.
 * @param color Line color.
 * @param markerType The specific type of marker you want to use, see MarkerTypes
 * @param markerSize The length of the marker axis [default = 20 pixels]
 * @param thickness Line thickness.
 * @param line_type Type of the line, See LineTypes
 */
export declare function drawMarker(img: InputOutputArray, position: Point, color: any, markerType?: number, markerSize?: number, thickness?: number, line_type?: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param img Image.
 * @param box Alternative ellipse representation via RotatedRect. This means that the function draws an ellipse inscribed in the rotated rectangle.
 * @param color Ellipse color.
 * @param thickness Thickness of the ellipse arc outline, if positive. Otherwise, this indicates that a filled ellipse sector is to be drawn.
 * @param lineType Type of the ellipse boundary. See LineTypes
 */
export declare function ellipse(img: InputOutputArray, box: any, color: any, thickness?: number, lineType?: number): void

/**
 * Draws a simple or thick elliptic arc or fills an ellipse sector.
 *
 * The function cv::ellipse with more parameters draws an ellipse outline, a filled ellipse, an elliptic arc, or a filled ellipse sector. The drawing code uses general parametric form. A piecewise-linear curve is used to approximate the elliptic arc boundary. If you need more control of the ellipse rendering, you can retrieve the curve using ellipse2Poly and then render it with polylines or fill it with fillPoly. If you use the first variant of the function and want to draw the whole ellipse, not an arc, pass `startAngle=0` and `endAngle=360`. If `startAngle` is greater than `endAngle`, they are swapped. The figure below explains the meaning of the parameters to draw the blue arc.
 *
 * @param img Image.
 * @param center Center of the ellipse.
 * @param axes Half of the size of the ellipse main axes.
 * @param angle Ellipse rotation angle in degrees.
 * @param startAngle Starting angle of the elliptic arc in degrees.
 * @param endAngle Ending angle of the elliptic arc in degrees.
 * @param color Ellipse color.
 * @param thickness Thickness of the ellipse arc outline, if positive. Otherwise, this indicates that a filled ellipse sector is to be drawn.
 * @param lineType Type of the ellipse boundary. See LineTypes
 * @param shift Number of fractional bits in the coordinates of the center and values of axes.
 */
export declare function ellipse(img: InputOutputArray, center: Point, axes: Size, angle: number, startAngle: number, endAngle: number, color: any, thickness?: number, lineType?: number, shift?: number): void

/**
 * Approximates an elliptic arc with a polyline.
 *
 * The function ellipse2Poly computes the vertices of a polyline that approximates the specified elliptic arc. It is used by ellipse. If `arcStart` is greater than `arcEnd`, they are swapped.
 *
 * @param center Center of the arc.
 * @param axes Half of the size of the ellipse main axes. See ellipse for details.
 * @param angle Rotation angle of the ellipse in degrees. See ellipse for details.
 * @param arcStart Starting angle of the elliptic arc in degrees.
 * @param arcEnd Ending angle of the elliptic arc in degrees.
 * @param delta Angle between the subsequent polyline vertices. It defines the approximation accuracy.
 * @param pts Output vector of polyline vertices.
 */
export declare function ellipse2Poly(center: Point, axes: Size, angle: number, arcStart: number, arcEnd: number, delta: number, pts: Vector<any>): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param center Center of the arc.
 * @param axes Half of the size of the ellipse main axes. See ellipse for details.
 * @param angle Rotation angle of the ellipse in degrees. See ellipse for details.
 * @param arcStart Starting angle of the elliptic arc in degrees.
 * @param arcEnd Ending angle of the elliptic arc in degrees.
 * @param delta Angle between the subsequent polyline vertices. It defines the approximation accuracy.
 * @param pts Output vector of polyline vertices.
 */
export declare function ellipse2Poly(center: Point2d, axes: Size2d, angle: number, arcStart: number, arcEnd: number, delta: number, pts: Vector<any>): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
export declare function fillConvexPoly(img: InputOutputArray, pts: any, npts: number, color: any, lineType?: number, shift?: number): void

/**
 * Fills a convex polygon.
 *
 * The function cv::fillConvexPoly draws a filled convex polygon. This function is much faster than the function fillPoly . It can fill not only convex polygons but any monotonic polygon without self-intersections, that is, a polygon whose contour intersects every horizontal line (scan line) twice at the most (though, its top-most and/or the bottom edge could be horizontal).
 *
 * @param img Image.
 * @param points Polygon vertices.
 * @param color Polygon color.
 * @param lineType Type of the polygon boundaries. See LineTypes
 * @param shift Number of fractional bits in the vertex coordinates.
 */
export declare function fillConvexPoly(img: InputOutputArray, points: InputArray, color: any, lineType?: number, shift?: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
export declare function fillPoly(img: InputOutputArray, pts: any, npts: any, ncontours: number, color: any, lineType?: number, shift?: number, offset?: Point): void

/**
 * Fills the area bounded by one or more polygons.
 *
 * The function cv::fillPoly fills an area bounded by several polygonal contours. The function can fill complex areas, for example, areas with holes, contours with self-intersections (some of their parts), and so forth.
 *
 * @param img Image.
 * @param pts Array of polygons where each polygon is represented as an array of points.
 * @param color Polygon color.
 * @param lineType Type of the polygon boundaries. See LineTypes
 * @param shift Number of fractional bits in the vertex coordinates.
 * @param offset Optional offset of all points of the contours.
 */
export declare function fillPoly(img: InputOutputArray, pts: InputArrayOfArrays, color: any, lineType?: number, shift?: number, offset?: Point): void

/**
 * Calculates the font-specific size to use to achieve a given height in pixels.
 *
 * @return The fontSize to use for cv::putText
 *
 * @see cv::putText
 *
 * @param fontFace Font to use, see cv::HersheyFonts.
 * @param pixelHeight Pixel height to compute the fontScale for
 * @param thickness Thickness of lines used to render the text.See putText for details.
 */
export declare function getFontScaleFromHeight(fontFace: any, pixelHeight: any, thickness?: any): number

/**
 * Draws a line segment connecting two points.
 *
 * The function line draws the line segment between pt1 and pt2 points in the image. The line is clipped by the image boundaries. For non-antialiased lines with integer coordinates, the 8-connected or 4-connected Bresenham algorithm is used. Thick lines are drawn with rounding endings. Antialiased lines are drawn using Gaussian filtering.
 *
 * @param img Image.
 * @param pt1 First point of the line segment.
 * @param pt2 Second point of the line segment.
 * @param color Line color.
 * @param thickness Line thickness.
 * @param lineType Type of the line. See LineTypes.
 * @param shift Number of fractional bits in the point coordinates.
 */
export declare function line(img: InputOutputArray, pt1: Point, pt2: Point, color: any, thickness?: number, lineType?: number, shift?: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
export declare function polylines(img: InputOutputArray, pts: any, npts: any, ncontours: number, isClosed: boolean, color: any, thickness?: number, lineType?: number, shift?: number): void

/**
 * Draws several polygonal curves.
 *
 * The function cv::polylines draws one or more polygonal curves.
 *
 * @param img Image.
 * @param pts Array of polygonal curves.
 * @param isClosed Flag indicating whether the drawn polylines are closed or not. If they are closed, the function draws a line from the last vertex of each curve to its first vertex.
 * @param color Polyline color.
 * @param thickness Thickness of the polyline edges.
 * @param lineType Type of the line segments. See LineTypes
 * @param shift Number of fractional bits in the vertex coordinates.
 */
export declare function polylines(img: InputOutputArray, pts: InputArrayOfArrays, isClosed: boolean, color: any, thickness?: number, lineType?: number, shift?: number): void

/**
 * Draws a text string.
 *
 * The function cv::putText renders the specified text string in the image. Symbols that cannot be rendered using the specified font are replaced by question marks. See getTextSize for a text rendering code example.
 *
 *
 * The `fontScale` parameter is a scale factor that is multiplied by the base font size:
 * - When scale > 1, the text is magnified.
 * - When 0 < scale < 1, the text is minimized.
 * - When scale < 0, the text is mirrored or reversed.
 *
 * @param img Image.
 * @param text Text string to be drawn.
 * @param org Bottom-left corner of the text string in the image.
 * @param fontFace Font type, see HersheyFonts.
 * @param fontScale Font scale factor that is multiplied by the font-specific base size.
 * @param color Text color.
 * @param thickness Thickness of the lines used to draw a text.
 * @param lineType Line type. See LineTypes
 * @param bottomLeftOrigin When true, the image data origin is at the bottom-left corner. Otherwise, it is at the top-left corner.
 */
export declare function putText(img: InputOutputArray, text: any, org: Point, fontFace: number, fontScale: number, color: Scalar, thickness?: number, lineType?: number, bottomLeftOrigin?: boolean): void

/**
 * Draws a text string using specified font.
 *
 * The function cv::putText renders the specified text string in the image. Symbols that cannot be rendered using the specified font are replaced by question marks. See getTextSize for a text rendering code example. The function returns the coordinates in pixels from where the text can be continued.
 *
 * @param img Image.
 * @param text Text string to be drawn.
 * @param org Bottom-left corner of the first character of the printed text (see PUT_TEXT_ALIGN_... though)
 * @param color Text color.
 * @param fface The font to use for the text
 * @param size Font size in pixels (by default) or pts
 * @param weight Font weight, 100..1000, where 100 is "thin" font, 400 is "regular", 600 is "semibold", 800 is "bold" and beyond that is "black". The parameter is ignored if the font is not a variable font or if it does not provide variation along 'wght' axis. If the weight is 0, then the weight, currently set via setInstance(), is used.
 * @param flags Various flags, see PUT_TEXT_...
 * @param wrap The optional text wrapping range: In the case of left-to-right (LTR) text if the printed character would cross wrap.end boundary, the "cursor" is set to wrap.start. In the case of right-to-left (RTL) text it's vice versa. If the parameters is not set, [org.x, img.cols] is used for LTR text and [0, org.x] is for RTL one.
 */
export declare function putText(img: InputOutputArray, text: any, org: Point, color: Scalar, fface: any, size: number, weight?: number, flags?: PutTextFlags, wrap?: Range): Point

/**
 * Draws a simple, thick, or filled up-right rectangle.
 *
 * The function cv::rectangle draws a rectangle outline or a filled rectangle whose two opposite corners are pt1 and pt2.
 *
 * @param img Image.
 * @param pt1 Vertex of the rectangle.
 * @param pt2 Vertex of the rectangle opposite to pt1 .
 * @param color Rectangle color or brightness (grayscale image).
 * @param thickness Thickness of lines that make up the rectangle. Negative values, like FILLED, mean that the function has to draw a filled rectangle.
 * @param lineType Type of the line. See LineTypes
 * @param shift Number of fractional bits in the point coordinates.
 */
export declare function rectangle(img: InputOutputArray, pt1: Point, pt2: Point, color: any, thickness?: number, lineType?: number, shift?: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 *
 * use `rec` parameter as alternative specification of the drawn rectangle: `r.tl() and r.br()-Point(1,1)` are opposite corners
 */
export declare function rectangle(img: InputOutputArray, rec: Rect, color: any, thickness?: number, lineType?: number, shift?: number): void

/**
 * normal size sans-serif font
 */
export declare const FONT_HERSHEY_SIMPLEX: number

/**
 * small size sans-serif font
 */
export declare const FONT_HERSHEY_PLAIN: number

/**
 * normal size sans-serif font (more complex than FONT_HERSHEY_SIMPLEX)
 */
export declare const FONT_HERSHEY_DUPLEX: number

/**
 * normal size serif font
 */
export declare const FONT_HERSHEY_COMPLEX: number

/**
 * normal size serif font (more complex than FONT_HERSHEY_COMPLEX)
 */
export declare const FONT_HERSHEY_TRIPLEX: number

/**
 * smaller version of FONT_HERSHEY_COMPLEX
 */
export declare const FONT_HERSHEY_COMPLEX_SMALL: number

/**
 * hand-writing style font
 */
export declare const FONT_HERSHEY_SCRIPT_SIMPLEX: number

/**
 * more complex variant of FONT_HERSHEY_SCRIPT_SIMPLEX
 */
export declare const FONT_HERSHEY_SCRIPT_COMPLEX: number

/**
 * flag for italic font
 */
export declare const FONT_ITALIC: number

export declare const FILLED: number

/**
 * 4-connected line
 */
export declare const LINE_4: number

/**
 * 8-connected line
 */
export declare const LINE_8: number

/**
 * antialiased line
 */
export declare const LINE_AA: number

/**
 * A crosshair marker shape.
 */
export declare const MARKER_CROSS: number

/**
 * A 45 degree tilted crosshair marker shape.
 */
export declare const MARKER_TILTED_CROSS: number

/**
 * A star marker shape, combination of cross and tilted cross.
 */
export declare const MARKER_STAR: number

/**
 * A diamond marker shape.
 */
export declare const MARKER_DIAMOND: number

/**
 * A square marker shape.
 */
export declare const MARKER_SQUARE: number

/**
 * An upwards pointing triangle marker shape.
 */
export declare const MARKER_TRIANGLE_UP: number

/**
 * A downwards pointing triangle marker shape.
 */
export declare const MARKER_TRIANGLE_DOWN: number

export declare const PUT_TEXT_ALIGN_LEFT: number

export declare const PUT_TEXT_ALIGN_CENTER: number

export declare const PUT_TEXT_ALIGN_RIGHT: number

export declare const PUT_TEXT_ALIGN_MASK: number

export declare const PUT_TEXT_ORIGIN_TL: number

export declare const PUT_TEXT_ORIGIN_BL: number

export declare const PUT_TEXT_WRAP: number
