// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { Mat } from './Mat';

/**
 * Kalman filter class.
 *
 * The class implements a standard Kalman filter http://en.wikipedia.org/wiki/Kalman_filter (http://en.wikipedia.org/wiki/Kalman_filter), Welch95 . However, you can modify transitionMatrix, controlMatrix, and measurementMatrix to get an extended Kalman filter functionality. @note In C API when CvKalman* kalmanFilter structure is not needed anymore, it should be released with cvReleaseKalman(&kalmanFilter)
 */
export declare class KalmanFilter {
/**
 * control matrix (B) (not used if there is no control)
 */
  public controlMatrix: Mat

/**
 * posteriori error estimate covariance matrix (P(k)): P(k)=(I-K(k)*H)*P'(k)
 */
  public errorCovPost: Mat

/**
 * priori error estimate covariance matrix (P'(k)): P'(k)=A*P(k-1)*At + Q)*\/
 */
  public errorCovPre: Mat

/**
 * Kalman gain matrix (K(k)): K(k)=P'(k)*Ht*inv(H*P'(k)*Ht+R)
 */
  public gain: Mat

/**
 * measurement matrix (H)
 */
  public measurementMatrix: Mat

/**
 * measurement noise covariance matrix (R)
 */
  public measurementNoiseCov: Mat

/**
 * process noise covariance matrix (Q)
 */
  public processNoiseCov: Mat

/**
 * corrected state (x(k)): x(k)=x'(k)+K(k)*(z(k)-H*x'(k))
 */
  public statePost: Mat

/**
 * predicted state (x'(k)): x(k)=A*x(k-1)+B*u(k)
 */
  public statePre: Mat

  public temp1: Mat

  public temp2: Mat

  public temp3: Mat

  public temp4: Mat

  public temp5: Mat

/**
 * state transition matrix (A)
 */
  public transitionMatrix: Mat

  public constructor()

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param dynamParams Dimensionality of the state.
 * @param measureParams Dimensionality of the measurement.
 * @param controlParams Dimensionality of the control vector.
 * @param type Type of the created matrices that should be CV_32F or CV_64F.
 */
  public constructor(dynamParams: number, measureParams: number, controlParams?: number, type?: number)

/**
 * Updates the predicted state from the measurement.
 *
 * @param measurement The measured system parameters
 */
  public correct(measurement: Mat): Mat

/**
 * Re-initializes Kalman filter. The previous content is destroyed.
 *
 * @param dynamParams Dimensionality of the state.
 * @param measureParams Dimensionality of the measurement.
 * @param controlParams Dimensionality of the control vector.
 * @param type Type of the created matrices that should be CV_32F or CV_64F.
 */
  public init(dynamParams: number, measureParams: number, controlParams?: number, type?: number): void

/**
 * Computes a predicted state.
 *
 * @param control The optional input control
 */
  public predict(control?: Mat): Mat
}
