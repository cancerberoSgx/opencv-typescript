// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray } from '../hacks/mat';
import type { Rect } from '../hacks/scalars';

/**
 * Base abstract class for the long-term tracker.
 */
export declare class Tracker {
/**
 * Return tracking score.
 */
  public getTrackingScore(): number

/**
 * Initialize the tracker with a known bounding box that surrounded the target.
 *
 * @param image The initial frame
 * @param boundingBox The initial bounding box
 */
  public init(image: InputArray, boundingBox: Rect): void

/**
 * Update the tracker, find the new most likely bounding box for the target.
 *
 * @return True means that target was located and false means that tracker cannot locate target in current frame. Note, that latter *does not* imply that tracker has failed, maybe target is indeed missing from the frame (say, out of sight)
 *
 * @param image The current frame
 * @param boundingBox The bounding box that represent the new target location, if true was returned, not modified otherwise
 */
  public update(image: InputArray, boundingBox: Rect): boolean
}
