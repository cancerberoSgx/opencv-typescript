// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray, OutputArray } from '../hacks/mat';
import type { Point } from '../hacks/scalars';

/**
 * Useful links:
 *
 *
 * https://www.learnopencv.com/seamless-cloning-using-opencv-python-cpp (https://www.learnopencv.com/seamless-cloning-using-opencv-python-cpp)
 */

/**
 * Given an original color image, two differently colored versions of this image can be mixed seamlessly.
 *
 * Multiplication factor is between .5 to 2.5.
 *
 * @param src Input 8-bit 3-channel image.
 * @param mask Input 8-bit 1 or 3-channel image.
 * @param dst Output image with the same size and type as src .
 * @param red_mul R-channel multiply factor.
 * @param green_mul G-channel multiply factor.
 * @param blue_mul B-channel multiply factor.
 */
export declare function colorChange(src: InputArray, mask: InputArray, dst: OutputArray, red_mul?: number, green_mul?: number, blue_mul?: number): void

/**
 * Applying an appropriate non-linear transformation to the gradient field inside the selection and then integrating back with a Poisson solver, modifies locally the apparent illumination of an image.
 *
 * This is useful to highlight under-exposed foreground objects or to reduce specular reflections.
 *
 * @param src Input 8-bit 3-channel image.
 * @param mask Input 8-bit 1 or 3-channel image.
 * @param dst Output image with the same size and type as src.
 * @param alpha Value ranges between 0-2.
 * @param beta Value ranges between 0-2.
 */
export declare function illuminationChange(src: InputArray, mask: InputArray, dst: OutputArray, alpha?: number, beta?: number): void

/**
 * Performs seamless cloning to blend a region from a source image into a destination image. This function is designed for local image editing, allowing changes restricted to a region (manually selected as the ROI) to be applied effortlessly and seamlessly. These changes can range from slight distortions to complete replacement by novel content PM03.
 *
 * @param src The source image (8-bit 3-channel), from which a region will be blended into the destination.
 * @param dst The destination image (8-bit 3-channel), where the src image will be blended.
 * @param mask A binary mask (8-bit, 1, 3, or 4-channel) specifying the region in the source image to blend. Non-zero pixels indicate the region to be blended. If an empty Mat is provided, a mask with all non-zero pixels is created internally.
 * @param p The point where the center of the src image is placed in the dst image.
 * @param blend The output image that stores the result of the seamless cloning. It has the same size and type as `dst`.
 * @param flags Flags that control the type of cloning method, can take values of `cv::SeamlessCloneFlags`.
 */
export declare function seamlessClone(src: InputArray, dst: InputArray, mask: InputArray, p: Point, blend: OutputArray, flags: number): void

/**
 * By retaining only the gradients at edge locations, before integrating with the Poisson solver, one washes out the texture of the selected region, giving its contents a flat aspect. Here Canny Edge Detector is used.
 *
 * @note The algorithm assumes that the color of the source image is close to that of the destination. This assumption means that when the colors don't match, the source image color gets tinted toward the color of the destination image.
 *
 * @param src Input 8-bit 3-channel image.
 * @param mask Input 8-bit 1 or 3-channel image.
 * @param dst Output image with the same size and type as src.
 * @param low_threshold Range from 0 to 100.
 * @param high_threshold Value > 100.
 * @param kernel_size The size of the Sobel kernel to be used.
 */
export declare function textureFlattening(src: InputArray, mask: InputArray, dst: OutputArray, low_threshold?: number, high_threshold?: number, kernel_size?: number): void

/**
 * Normal seamless cloning. This method is ideal for inserting objects with complex outlines into a new background. It preserves the original appearance and lighting of the inserted object, ensuring a natural blend.
 */
export declare const NORMAL_CLONE: number

/**
 * Mixed seamless cloning. This method addresses cases where simple color-based selection or alpha masking is time-consuming and may result in undesirable halos. By combining structure from the source and texture from the destination, mixed seamless cloning is highly effective, even with loosely defined selections.
 */
export declare const MIXED_CLONE: number

/**
 * Monochrome transfer cloning. This method allows users to replace specific features of an object, such as grayscale textures or patterns, with alternative features. It is particularly useful for artistic effects or targeted object modifications.
 */
export declare const MONOCHROME_TRANSFER: number

/**
 * Enhanced normal seamless cloning. Similar to `NORMAL_CLONE`, but with an advanced approach to ROI (Region of Interest) calculation. This mode processes a larger source region by considering the entire mask area instead of only the bounding rectangle of non-zero pixels.
 */
export declare const NORMAL_CLONE_WIDE: number

/**
 * Enhanced mixed seamless cloning. Similar to `MIXED_CLONE`, but with an advanced approach to ROI (Region of Interest) calculation. This mode processes a larger source region by considering the entire mask area instead of only the bounding rectangle of non-zero pixels.
 */
export declare const MIXED_CLONE_WIDE: number

/**
 * Enhanced monochrome transfer cloning. Similar to `MONOCHROME_TRANSFER`, but with an advanced approach to ROI (Region of Interest) calculation. This mode processes a larger source region by considering the entire mask area instead of only the bounding rectangle of non-zero pixels.
 */
export declare const MONOCHROME_TRANSFER_WIDE: number
