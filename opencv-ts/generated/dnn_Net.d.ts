// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { Vector } from '../hacks/runtime';
import type { Scalar } from '../hacks/scalars';
import type { Mat } from './Mat';
import type { Arg, ArgData, ArgKind, AsyncArray, CV_ND, CV_WRAP_FILE_PATH, LayerId, LayerParams, MatShape, ModelFormat, Net, ProfilingMode, Ptr, TracingMode } from './_unresolved';

/**
 * This class allows to create and manipulate comprehensive artificial neural networks.
 *
 * Neural network is presented as directed acyclic graph (DAG), where vertices are Layer instances, and edges specify relationships between layers inputs and outputs.
 *
 *
 * Each network layer has unique integer id and unique string name inside its network. LayerId can store either layer name or layer id.
 *
 *
 * This class supports reference counting of its instances, i. e. copies point to the same instance.
 */
export declare class dnn_Net {
/**
 * Default constructor.
 */
  public Net(): any

/**
 * Adds new layer to the net.
 *
 * @return unique identifier of created layer, or -1 if a failure will happen.
 *
 * @param name unique name of the adding layer.
 * @param type typename of the adding layer (type must be registered in LayerRegister).
 * @param dtype datatype of output blobs.
 * @param params parameters which will be used to initialize the creating layer.
 */
  public addLayer(name: String, type: String, dtype: any, params: LayerParams): number

  public addLayer(name: String, type: String, params: LayerParams): number

/**
 * Adds new layer and connects its first input to the first output of previously added layer.
 *
 * @see addLayer()
 */
  public addLayerToPrev(name: String, type: String, dtype: any, params: LayerParams): number

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public addLayerToPrev(name: String, type: String, params: LayerParams): number

  public argData(arg: Arg): ArgData

  public argKind(arg: Arg): ArgKind

  public argName(arg: Arg): any

  public argTensor(arg: Arg): Mat

  public argType(arg: Arg): number

/**
 * Connects #`outNum` output of the first layer to #`inNum` input of the second layer.
 *
 * @param outLayerId identifier of the first layer
 * @param outNum number of the first layer output
 * @param inpLayerId identifier of the second layer
 * @param inpNum number of the second layer input
 */
  public connect(outLayerId: number, outNum: number, inpLayerId: number, inpNum: number): void

/**
 * Connects output of the first layer to input of the second layer.
 *
 * Descriptors have the following template `<layer_name>[.input_number]`:
 * - the first part of the template `layer_name` is string name of the added layer. If this part is empty then the network input pseudo layer will be used;
 * - the second optional part of the template `input_number` is either number of the layer input, either label one. If this part is omitted then the first layer input will be used.
 *
 *
 * @see setNetInputs(), Layer::inputNameToIndex(), Layer::outputNameToIndex()
 *
 * @param outPin descriptor of the first layer output.
 * @param inpPin descriptor of the second layer input.
 */
  public connect(outPin: String, inpPin: String): void

/**
 * Disables KV-Cache for all AttentionOnnxI layers.
 */
  public disableKVCache(): void

/**
 * Dump net to String.
 *
 * @return String with structure, hyperparameters, backend, target and fusion Call method after setInput(). To see correct backend, target and fusion run after forward().
 */
  public dump(): String

  public dumpArg(strm: any, arg: Arg, indent: number, comma?: boolean, dump_details?: boolean): any

  public dumpDim(strm: any, value: number): any

/**
 * Dump net structure, hyperparameters, backend, target and fusion to dot file.
 *
 * @see dump()
 *
 * @param path path to output file with .dot extension
 */
  public dumpToFile(path: CV_WRAP_FILE_PATH): void

/**
 * Dump net structure, hyperparameters, backend, target and fusion to pbtxt file.
 *
 * Use Netron (https://netron.app (https://netron.app)) to open the target file to visualize the model. Call method after setInput(). To see correct backend, target and fusion run after forward().
 *
 * @param path path to output file with .pbtxt extension
 */
  public dumpToPbtxt(path: CV_WRAP_FILE_PATH): void

/**
 * Dump net structure, hyperparameters, backend, target and fusion to the specified output stream.
 *
 * @param strm the target stream
 */
  public dumpToStream(strm: any): void

/**
 * Returns true if there are no layers in the network.
 */
  public empty(): boolean

/**
 * Enables or disables layer fusion in the network.
 *
 * @param fusion true to enable the fusion, false to disable. The fusion is enabled by default.
 */
  public enableFusion(fusion: boolean): void

/**
 * Enables KV-Cache for all AttentionOnnxI layers.
 */
  public enableKVCache(): void

/**
 * Enables or disables the Winograd compute branch. The Winograd compute branch can speed up 3x3 Convolution at a small loss of accuracy.
 *
 * @param useWinograd true to enable the Winograd compute branch. The default is true.
 */
  public enableWinograd(useWinograd: boolean): void

/**
 * Finalizes the network configuration and prepares it for inference.
 *
 * This method must be called after setting backend/target via setPreferableBackend() and setPreferableTarget(), and before the first forward() call. It creates the underlying execution session (e.g. ONNX Runtime session) on the configured backend/target. If not called explicitly, the first forward() will call it automatically.
 *
 *
 * Calling finalizeNet() early lets you pay the one-time setup cost at a predictable point and catch configuration errors before inference.
 */
  public finalizeNet(): void

  public findDim(name: any, insert?: boolean): number

/**
 * Runs forward pass to compute output of layer with name `outputName`.
 *
 * @return blob for first output of specified layer.
 *
 * By default runs forward pass for the whole network.
 *
 * @param outputName name for layer which output is needed to get
 */
  public forward(outputName?: String): Mat

/**
 * Runs forward pass to compute outputs of layers listed in `outBlobNames`.
 *
 * @param outputBlobs contains blobs for first outputs of specified layers.
 * @param outBlobNames names for layers which outputs are needed to get
 */
  public forward(outputBlobs: CV_ND, outBlobNames: String): void

/**
 * Runs forward pass to compute output of layer with name `outputName`.
 *
 * If `outputName` is empty, runs forward pass for the whole network.
 *
 * @param outputBlobs contains all output blobs for specified layer.
 * @param outputName name for layer which output is needed to get
 */
  public forward(outputBlobs: CV_ND, outputName?: String): void

/**
 * Runs forward pass to compute outputs of layers listed in `outBlobNames`.
 *
 * @param outputBlobs contains all output blobs for each layer specified in `outBlobNames`.
 * @param outBlobNames names for layers which outputs are needed to get
 */
  public forward(outputBlobs: Vector<any>, outBlobNames: String): void

/**
 * Runs forward pass to compute output of layer with name `outputName`.
 *
 * By default runs forward pass for the whole network.
 *
 *
 * This is an asynchronous version of forward(const String&). dnn::DNN_BACKEND_INFERENCE_ENGINE backend is required.
 *
 * @param outputName name for layer which output is needed to get
 */
  public forwardAsync(outputName?: String): AsyncArray

  public getArg(name: any): Arg

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public getFLOPS(layerId: any, netInputShape: MatShape, netInputType: any): number

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public getFLOPS(layerId: any, netInputShapes: MatShape, netInputTypes: any): number

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts. These overloads should be deprecated
 */
  public getFLOPS(netInputShape: MatShape, netInputType: any): number

/**
 * Computes FLOP for whole loaded model with specified input shapes.
 *
 * @return computed FLOP.
 *
 * @param netInputShapes vector of shapes for all net inputs.
 * @param netInputTypes vector of types for all net inputs.
 */
  public getFLOPS(netInputShapes: MatShape, netInputTypes: any): number

  public getImpl(): any

  public getImplRef(): any

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 *
 * Deprecatedto be removed
 */
  public getLayer(layerId: LayerId): Ptr

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 *
 * DeprecatedUse int getLayerId(const String &layer)
 */
  public getLayer(layerName: String): Ptr

/**
 * Returns pointer to layer with specified id or name which the network use.
 */
  public getLayer(layerId: number): Ptr

/**
 * Converts string name of the layer to the integer identifier.
 *
 * @return id of the layer, or -1 if the layer wasn't found.
 */
  public getLayerId(layer: String): number

/**
 * Returns pointers to input layers of specific layer.
 */
  public getLayerInputs(layerId: number): Vector<any>

  public getLayerNames(): Vector<any>

/**
 * Returns count of layers of specified type.
 *
 * @return count of layers
 *
 * @param layerType type.
 */
  public getLayersCount(layerType: String): number

/**
 * Returns input and output shapes for layer with specified id in loaded model; preliminary inferencing isn't necessary.
 *
 * This overload should be deprecated
 *
 * @param netInputShape shape input blob in net input layer.
 * @param netInputType input type in net input layer.
 * @param layerId id for layer.
 * @param inLayerShapes output parameter for input layers shapes; order is the same as in layersIds
 * @param outLayerShapes output parameter for output layers shapes; order is the same as in layersIds
 */
  public getLayerShapes(netInputShape: MatShape, netInputType: any, layerId: any, inLayerShapes: Vector<any>, outLayerShapes: Vector<any>): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 *
 * The only overload of getLayerShapes that should be kept in 5.x
 */
  public getLayerShapes(netInputShapes: MatShape, netInputTypes: any, layerId: any, inLayerShapes: Vector<any>, outLayerShapes: Vector<any>): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 *
 * This overload should be deprecated
 */
  public getLayersShapes(netInputShape: MatShape, netInputType: any, layersIds: Vector<any>, inLayersShapes: Vector<any>, outLayersShapes: Vector<any>): void

/**
 * Returns input and output shapes for all layers in loaded model; preliminary inferencing isn't necessary.
 *
 * This overload should be deprecated
 *
 * @param netInputShapes shapes for all input blobs in net input layer.
 * @param netInputTypes types for all input blobs in net input layer.
 * @param layersIds output parameter for layer IDs.
 * @param inLayersShapes output parameter for input layers shapes; order is the same as in layersIds
 * @param outLayersShapes output parameter for output layers shapes; order is the same as in layersIds.
 */
  public getLayersShapes(netInputShapes: MatShape, netInputTypes: any, layersIds: Vector<any>, inLayersShapes: Vector<any>, outLayersShapes: Vector<any>): void

/**
 * Returns list of types for layer used in model.
 *
 * @param layersTypes output parameter for returning types.
 */
  public getLayerTypes(layersTypes: Vector<any>): void

  public getMainGraph(): Ptr

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts. It should be deprecated
 */
  public getMemoryConsumption(layerId: any, netInputShape: MatShape, netInputType: any, weights: any, blobs: any): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts. It should be deprecated
 */
  public getMemoryConsumption(layerId: any, netInputShapes: MatShape, netInputTypes: any, weights: any, blobs: any): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts. It should be deprecated
 */
  public getMemoryConsumption(netInputShape: MatShape, netInputType: any, weights: any, blobs: any): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 *
 * It should be deprecated
 */
  public getMemoryConsumption(netInputShape: MatShape, netInputType: any, layerIds: Vector<any>, weights: Vector<any>, blobs: Vector<any>): void

/**
 * Computes bytes number which are required to store all weights and intermediate blobs for model.
 *
 * @param netInputShapes vector of shapes for all net inputs.
 * @param netInputTypes vector of types for all net inputs.
 * @param weights output parameter to store resulting bytes for weights.
 * @param blobs output parameter to store resulting bytes for intermediate blobs.
 */
  public getMemoryConsumption(netInputShapes: MatShape, netInputTypes: any, weights: any, blobs: any): void

/**
 * Computes bytes number which are required to store all weights and intermediate blobs for each layer.
 *
 * It should be deprecated
 *
 * @param netInputShapes vector of shapes for all net inputs.
 * @param netInputTypes vector of types for all net inputs.
 * @param layerIds output vector to save layer IDs.
 * @param weights output parameter to store resulting bytes for weights.
 * @param blobs output parameter to store resulting bytes for intermediate blobs.
 */
  public getMemoryConsumption(netInputShapes: MatShape, netInputTypes: any, layerIds: Vector<any>, weights: Vector<any>, blobs: Vector<any>): void

/**
 * Retrieve the current model format, see DNN_MODEL_*.
 */
  public getModelFormat(): ModelFormat

  public getParam(layerName: String, numParam?: number): Mat

/**
 * Returns parameter blob of the layer.
 *
 * @see Layer::blobs
 *
 * @param layer name or id of the layer.
 * @param numParam index of the layer parameter in the Layer::blobs array.
 */
  public getParam(layer: number, numParam?: number): Mat

/**
 * Returns overall time for inference and timings (in ticks) for layers.
 *
 * Indexes in returned vector correspond to layers ids. Some layers can be fused with others, in this case zero ticks count will be return for that skipped layers. Supported by DNN_BACKEND_OPENCV on DNN_TARGET_CPU only.
 *
 *
 * @return overall ticks for model inference.
 *
 * @param timings vector for tick timings for all layers.
 */
  public getPerfProfile(timings: Vector<any>): number

/**
 * Returns profiling data captured during the last forward pass.
 *
 * Entries are sorted by time in descending order. Empty vectors are returned if profiling is disabled (DNN_PROFILE_NONE).
 */
  public getPerfProfile(names: Vector<any>, timems: Vector<any>, counts: Vector<any>): void

/**
 * Retrieve the current profiling mode.
 */
  public getProfilingMode(): ProfilingMode

/**
 * Retrieve the current tracing mode.
 */
  public getTracingMode(): TracingMode

/**
 * Returns indexes of layers with unconnected outputs.
 *
 * FIXIT: Rework API to registerOutput() approach, deprecate this call
 */
  public getUnconnectedOutLayers(): Vector<any>

/**
 * Returns names of layers with unconnected outputs.
 *
 * FIXIT: Rework API to registerOutput() approach, deprecate this call
 */
  public getUnconnectedOutLayersNames(): Vector<any>

  public haveArg(name: any): boolean

  public isConstArg(arg: Arg): boolean

/**
 * Prints the profile captured during the last forward pass in a formatted table using CV_LOG_INFO.
 *
 * In DNN_PROFILE_DETAILED mode, prints per-layer label, time, and percentage. In DNN_PROFILE_SUMMARY mode, prints per-type count, time, and percentage. Does nothing if profiling is disabled (DNN_PROFILE_NONE) or all timings are zero.
 */
  public printPerfProfile(): void

/**
 * Registers network output with name.
 *
 * Function may create additional 'Identity' layer.
 *
 *
 * @return index of bound layer (the same as layerId or newly created)
 *
 * @param outputName identifier of the output
 * @param layerId identifier of the second layer
 * @param outputPort number of the second layer input
 */
  public registerOutput(outputName: any, layerId: number, outputPort: number): number

/**
 * Pre-allocates KV-Cache pages for up to `maxSequenceLength` tokens.
 *
 * Call after enableKVCache() and before the prefill forward, so no page allocation happens during generation. A hint, not a limit: going past `maxSequenceLength` still grows the cache. resetKVCache() drops the reservation.
 *
 *
 * Only models whose attention imports as a single fused op (ONNX Attention, com.microsoft MultiHeadAttention / GroupQueryAttention) use the paged cache; otherwise this warns and does nothing.
 */
  public reserveKVCache(maxSequenceLength: number): void

/**
 * Resets KV-Cache for all AttentionOnnxI layers.
 */
  public resetKVCache(): void

/**
 * Sets the new input value for the network.
 *
 * @see connect(String, String) to know format of the descriptor.
 *
 * If scale or mean values are specified, a final input blob is computed as:
 *
 * @param blob A new blob. Should have CV_32F or CV_8U depth.
 * @param name A name of input layer.
 * @param scalefactor An optional normalization scale.
 * @param mean An optional mean subtraction values.
 */
  public setInput(blob: CV_ND, name?: String, scalefactor?: number, mean?: Scalar): void

/**
 * Specify shape of network input.
 */
  public setInputShape(inputName: String, shape: MatShape): void

/**
 * Sets outputs names of the network input pseudo layer.
 *
 * Each net always has special own the network input pseudo layer with id=0. This layer stores the user blobs only and don't make any computations. In fact, this layer provides the only way to pass user data into the network. As any other layer, this layer can label its outputs and this function provides an easy way to do this.
 */
  public setInputsNames(inputBlobNames: String): void

/**
 * Sets the parameter blob of a layer identified by its name or output tensor name.
 *
 * @param layerName raw ONNX output tensor name (ENGINE_OPENCV).
 * @param numParam index of the constant weight input to update (0 = kernel, 1 = bias, etc.).
 * @param blob the new parameter value.
 */
  public setParam(layerName: String, numParam: number, blob: CV_ND): void

/**
 * Sets the new value for the learned param of the layer.
 *
 * @see Layer::blobs
 *
 * @note If shape of the new blob differs from the previous shape, then the following forward pass may fail.
 *
 * @param layer name or id of the layer.
 * @param numParam index of the layer parameter in the Layer::blobs array.
 * @param blob the new value.
 */
  public setParam(layer: number, numParam: number, blob: CV_ND): void

/**
 * Ask network to use specific computation backend where it supported.
 *
 * @see Backend
 *
 * @param backendId backend identifier.
 */
  public setPreferableBackend(backendId: number): void

/**
 * Ask network to make computations on specific target device.
 *
 * @see Target
 *
 * List of supported combinations backend / target:
 *
 *
 *
 * DNN_BACKEND_OPENCV
 *
 *
 * DNN_BACKEND_INFERENCE_ENGINE
 *
 *
 * DNN_BACKEND_CUDA
 *
 *
 *
 *
 * DNN_TARGET_CPU
 *
 *
 * +
 *
 *
 * +
 *
 *
 *
 *
 *
 *
 *
 * DNN_TARGET_OPENCL
 *
 *
 * +
 *
 *
 * +
 *
 *
 *
 *
 *
 *
 *
 * DNN_TARGET_OPENCL_FP16
 *
 *
 * +
 *
 *
 * +
 *
 *
 *
 *
 *
 *
 *
 * DNN_TARGET_MYRIAD
 *
 *
 *
 *
 *
 * +
 *
 *
 *
 *
 *
 *
 *
 * DNN_TARGET_FPGA
 *
 *
 *
 *
 *
 * +
 *
 *
 *
 *
 *
 *
 *
 * DNN_TARGET_CUDA
 *
 *
 *
 *
 *
 *
 *
 *
 * +
 *
 *
 *
 *
 * DNN_TARGET_CUDA_FP16
 *
 *
 *
 *
 *
 *
 *
 *
 * +
 *
 *
 *
 *
 * DNN_TARGET_HDDL
 *
 *
 *
 *
 *
 * +
 *
 * @param targetId target identifier.
 */
  public setPreferableTarget(targetId: number): void

/**
 * Set the profiling mode.
 *
 * @param profilingMode the profiling mode, see DNN_PROFILE_*
 */
  public setProfilingMode(profilingMode: ProfilingMode): void

/**
 * Set the tracing mode.
 *
 * @param tracingMode the tracing mode, see DNN_TRACE_*
 */
  public setTracingMode(tracingMode: TracingMode): void

/**
 * Create a network from Intel's Model Optimizer in-memory buffers with intermediate representation (IR).
 *
 * @return Net object.
 *
 * @param bufferModelConfig buffer with model's configuration.
 * @param bufferWeights buffer with model's trained weights.
 */
  public static readFromModelOptimizer(bufferModelConfig: number, bufferWeights: number): Net

/**
 * Create a network from Intel's Model Optimizer in-memory buffers with intermediate representation (IR).
 *
 * @return Net object.
 *
 * @param bufferModelConfigPtr buffer pointer of model's configuration.
 * @param bufferModelConfigSize buffer size of model's configuration.
 * @param bufferWeightsPtr buffer pointer of model's trained weights.
 * @param bufferWeightsSize buffer size of model's trained weights.
 */
  public static readFromModelOptimizer(bufferModelConfigPtr: number, bufferModelConfigSize: number, bufferWeightsPtr: number, bufferWeightsSize: number): Net

/**
 * Create a network from Intel's Model Optimizer intermediate representation (IR).
 *
 * @param xml XML configuration file with network's topology.
 * @param bin Binary file with trained weights. Networks imported from Intel's Model Optimizer are launched in Intel's Inference Engine backend.
 */
  public static readFromModelOptimizer(xml: CV_WRAP_FILE_PATH, bin: CV_WRAP_FILE_PATH): Net
}
