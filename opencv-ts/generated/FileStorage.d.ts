// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { FileNode } from './FileNode';
import type { Mat } from './Mat';
import type { Ptr } from './_unresolved';

/**
 * XML/YAML/JSON file storage class that encapsulates all the information necessary for writing or reading data to/from a file.
 */
export declare class FileStorage {
  public elname: any

  public p: Ptr

  public state: number

/**
 * The constructors.
 *
 * The full constructor opens the file. Alternatively you can use the default constructor and then call FileStorage::open.
 */
  public constructor()

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 *
 * Opens a file.
 *
 *
 * See description of parameters in FileStorage::FileStorage. The method calls FileStorage::release before opening the file.
 *
 * @param filename Name of the file to open or the text string to read the data from. Extension of the file (.xml, .yml/.yaml or .json) determines its format (XML, YAML or JSON respectively). Also you can append .gz to work with compressed files, for example myHugeMatrix.xml.gz. You can also specify a compression level from 0 to 9 by appending it to the extension (e.g. ".gz0" for no compression, ".gz9" for high compression). The last digit will be truncated internally to write/read. (e.g. If "a.xml.gz9" is specified, "a.xml.gz" is used for the actual file name.) If both FileStorage::WRITE and FileStorage::MEMORY flags are specified, source is used just to specify the output file format (e.g. mydata.xml, .yml etc.). A file name can also contain parameters. You can use this format, "*?base64" (e.g. "file.json?base64" (case sensitive)), as an alternative to FileStorage::BASE64 flag.
 * @param flags Mode of operation. One of FileStorage::Mode
 * @param encoding Encoding of the file. Note that UTF-16 XML encoding is not supported currently and you should use 8-bit encoding instead of it.
 */
  public constructor(filename: String, flags: number, encoding?: String)

/**
 * Finishes writing nested structure (should pair startWriteStruct())
 */
  public endWriteStruct(): void

/**
 * Returns the first element of the top-level mapping.
 *
 * @return The first element of the top-level mapping.
 */
  public getFirstTopLevelNode(): FileNode

/**
 * Returns the current format.
 *
 * @return The current format, see FileStorage::Mode
 */
  public getFormat(): number

/**
 * Checks whether the file is opened.
 *
 * @return true if the object is associated with the current file and false otherwise. It is a good practice to call this method after you tried to open a file.
 */
  public isOpened(): boolean

/**
 * Opens a file.
 *
 * See description of parameters in FileStorage::FileStorage. The method calls FileStorage::release before opening the file.
 *
 * @param filename Name of the file to open or the text string to read the data from. Extension of the file (.xml, .yml/.yaml or .json) determines its format (XML, YAML or JSON respectively). Also you can append .gz to work with compressed files, for example myHugeMatrix.xml.gz. You can also specify a compression level from 0 to 9 by appending it to the extension (e.g. ".gz0" for no compression, ".gz9" for high compression). The last digit will be truncated internally to write/read. (e.g. If "a.xml.gz9" is specified, "a.xml.gz" is used for the actual file name.) If both FileStorage::WRITE and FileStorage::MEMORY flags are specified, source is used just to specify the output file format (e.g. mydata.xml, .yml etc.). A file name can also contain parameters. You can use this format, "*?base64" (e.g. "file.json?base64" (case sensitive)), as an alternative to FileStorage::BASE64 flag.
 * @param flags Mode of operation. One of FileStorage::Mode
 * @param encoding Encoding of the file. Note that UTF-16 XML encoding is not supported currently and you should use 8-bit encoding instead of it.
 */
  public open(filename: String, flags: number, encoding?: String): boolean

/**
 * Closes the file and releases all the memory buffers.
 *
 * Call this method after all I/O operations with the storage are finished.
 */
  public release(): void

/**
 * Closes the file and releases all the memory buffers.
 *
 * Call this method after all I/O operations with the storage are finished. If the storage was opened for writing data and FileStorage::WRITE was specified
 */
  public releaseAndGetString(): String

/**
 * Returns the top-level mapping.
 *
 * @return The top-level mapping.
 *
 * @param streamidx Zero-based index of the stream. In most cases there is only one stream in the file. However, YAML supports multiple streams and so there can be several.
 */
  public root(streamidx?: number): FileNode

/**
 * Starts to write a nested structure (sequence or a mapping).
 *
 * @param name name of the structure. When writing to sequences (a.k.a. "arrays"), pass an empty string.
 * @param flags type of the structure (FileNode::MAP or FileNode::SEQ (both with optional FileNode::FLOW)).
 * @param typeName optional name of the type you store. The effect of setting this depends on the storage format. I.e. if the format has a specification for storing type information, this parameter is used.
 */
  public startWriteStruct(name: String, flags: number, typeName?: String): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public write(name: String, val: boolean): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public write(name: String, val: Mat): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public write(name: String, val: String): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public write(name: String, val: String): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public write(name: String, val: number): void

/**
 * Simplified writing API to use with bindings.
 *
 * @param name Name of the written object. When writing to sequences (a.k.a. "arrays"), pass an empty string.
 * @param val Value of the written object.
 */
  public write(name: String, val: number): void

  public write(name: String, val: number): void

/**
 * Writes a comment.
 *
 * The function writes a comment into file storage. The comments are skipped when the storage is read.
 *
 * @param comment The written comment, single-line or multi-line
 * @param append If true, the function tries to put the comment at the end of current line. Else if the comment is multi-line, or if it does not fit at the end of the current line, the comment starts a new line.
 */
  public writeComment(comment: String, append?: boolean): void

/**
 * Writes multiple numbers.
 *
 * Writes one or more numbers of the specified format to the currently written structure. Usually it is more convenient to use operator `<<` instead of this method.
 *
 * @param fmt Specification of each array element, see format specification
 * @param vec Pointer to the written array.
 * @param len Number of the uchar elements to write.
 */
  public writeRaw(fmt: String, vec: any, len: number): void

/**
 * Returns the normalized object name for the specified name of a file.
 *
 * @return The normalized object name.
 *
 * @param filename Name of a file
 */
  public static getDefaultObjectName(filename: String): String
}

export type FileStorage_Mode = number

/**
 * value, open the file for reading
 */
export declare const FileStorage_READ: FileStorage_Mode

/**
 * value, open the file for writing
 */
export declare const FileStorage_WRITE: FileStorage_Mode

/**
 * value, open the file for appending
 */
export declare const FileStorage_APPEND: FileStorage_Mode

/**
 * flag, read data from source or write data to the internal buffer (which is returned by FileStorage::release)
 */
export declare const FileStorage_MEMORY: FileStorage_Mode

/**
 * mask for format flags
 */
export declare const FileStorage_FORMAT_MASK: FileStorage_Mode

/**
 * flag, auto format
 */
export declare const FileStorage_FORMAT_AUTO: FileStorage_Mode

/**
 * flag, XML format
 */
export declare const FileStorage_FORMAT_XML: FileStorage_Mode

/**
 * flag, YAML format
 */
export declare const FileStorage_FORMAT_YAML: FileStorage_Mode

/**
 * flag, JSON format
 */
export declare const FileStorage_FORMAT_JSON: FileStorage_Mode

/**
 * flag, Legacy YAML 1.0 format (strict headers, booleans as ints)
 */
export declare const FileStorage_FORMAT_YAML_1_0: FileStorage_Mode

/**
 * flag, write rawdata in Base64 by default. (consider using WRITE_BASE64)
 */
export declare const FileStorage_BASE64: FileStorage_Mode

/**
 * flag, enable both WRITE and BASE64
 */
export declare const FileStorage_WRITE_BASE64: FileStorage_Mode

export type FileStorage_State = number

/**
 * Initial or uninitialized state.
 */
export declare const FileStorage_UNDEFINED: FileStorage_State

/**
 * Expecting a value in the current position.
 */
export declare const FileStorage_VALUE_EXPECTED: FileStorage_State

/**
 * Expecting a key/name in the current position.
 */
export declare const FileStorage_NAME_EXPECTED: FileStorage_State

/**
 * Indicates being inside a map (a set of key-value pairs).
 */
export declare const FileStorage_INSIDE_MAP: FileStorage_State
