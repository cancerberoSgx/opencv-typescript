// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray } from '../hacks/mat';
import type { KeyPoint, Size } from '../hacks/scalars';
import type { DescriptorMatcher } from './DescriptorMatcher';
import type { Ptr } from './_unresolved';

/**
 * LightGlue feature matcher.
 *
 * LightGlue is a CNN-based feature matcher, as described in Lindenberger23 . It takes keypoint locations and descriptors from two images and directly predicts match pairs. Unlike traditional matchers that compute descriptor distances, LightGlue uses attention mechanisms to produce confidence scores for each potential match pair.
 *
 *
 * The matcher extends DescriptorMatcher and supports the standard match(), knnMatch(), and radiusMatch() interfaces. Context (keypoints and image sizes) must be provided via setPairInfo() before matching.
 */
export declare class LightGlueMatcher extends DescriptorMatcher {
/**
 * Clears stored pair context information.
 */
  public clearPairInfo(): void

/**
 * Convenience overload of setPairInfo() taking keypoints directly.
 */
  public setImagePairInfo(queryKpts: KeyPoint, trainKpts: KeyPoint, queryImageSize?: Size, trainImageSize?: Size): void

/**
 * Sets the keypoint and image size context for the next match() call.
 *
 * This provides the spatial context that LightGlue needs in addition to descriptors. Must be called before match()/knnMatch()/radiusMatch() unless using automatic context from in-process ALIKED instances.
 *
 * @param queryKpts Query image keypoints (Nx2 float matrix with x,y coordinates).
 * @param trainKpts Train image keypoints (Nx2 float matrix with x,y coordinates).
 * @param queryImageSize Size of the query image (width, height).
 * @param trainImageSize Size of the train image (width, height).
 */
  public setPairInfo(queryKpts: InputArray, trainKpts: InputArray, queryImageSize?: Size, trainImageSize?: Size): void

/**
 * Creates LightGlueMatcher from a model file path.
 *
 * @param modelPath Path to the ONNX model file.
 * @param scoreThreshold Match confidence threshold.
 * @param backend DNN backend
 * @param target DNN target
 */
  public static create(modelPath: String, scoreThreshold?: number, backend?: number, target?: number): Ptr
}
