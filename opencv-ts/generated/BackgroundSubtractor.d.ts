// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray, OutputArray } from '../hacks/mat';
import type { Algorithm } from './Algorithm';

/**
 * Base class for background/foreground segmentation. :
 *
 * The class is only used to define the common interface for the whole family of background/foreground segmentation algorithms.
 */
export declare class BackgroundSubtractor extends Algorithm {
/**
 * Computes a foreground mask with known foreground mask input.
 *
 * @note This method has a default virtual implementation that throws a "not impemented" error. Foreground masking may not be supported by all background subtractors.
 *
 * @param image Next video frame. Floating point frame will be used without scaling and should be in range .
 * @param knownForegroundMask The mask for inputting already known foreground, allows model to ignore pixels.
 * @param fgmask The output foreground mask as an 8-bit binary image.
 * @param learningRate The value between 0 and 1 that indicates how fast the background model is learnt. Negative parameter value makes the algorithm to use some automatically chosen learning rate. 0 means that the background model is not updated at all, 1 means that the background model is completely reinitialized from the last frame.
 */
  public apply(image: InputArray, knownForegroundMask: InputArray, fgmask: OutputArray, learningRate?: number): void

/**
 * Computes a foreground mask.
 *
 * @param image Next video frame.
 * @param fgmask The output foreground mask as an 8-bit binary image.
 * @param learningRate The value between 0 and 1 that indicates how fast the background model is learnt. Negative parameter value makes the algorithm to use some automatically chosen learning rate. 0 means that the background model is not updated at all, 1 means that the background model is completely reinitialized from the last frame.
 */
  public apply(image: InputArray, fgmask: OutputArray, learningRate?: number): void

/**
 * Computes a background image.
 *
 * @note Sometimes the background image can be very blurry, as it contain the average background statistics.
 *
 * @param backgroundImage The output background image.
 */
  public getBackgroundImage(backgroundImage: OutputArray): void
}
