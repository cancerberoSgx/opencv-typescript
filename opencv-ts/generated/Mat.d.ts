// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray, Mat_, OutputArray } from '../hacks/mat';
import type { MatStep, Point, Range, Rect, Scalar, Size } from '../hacks/scalars';
import type { AccessFlag, CV_NODISCARD_STD, MatAllocator, MatCommaInitializer_, MatConstIterator_, MatExpr, MatIterator_, MatShape, Matx, Point3_, Point_, UMat, UMatData, UMatUsageFlags, Vec } from './_unresolved';

/**
 * n-dimensional dense array class
 *
 * The class Mat represents an n-dimensional dense numerical single-channel or multi-channel array. It can be used to store real or complex-valued vectors and matrices, grayscale or color images, voxel volumes, vector fields, point clouds, tensors, histograms (though, very high-dimensional histograms may be better stored in a SparseMat ). The data layout of the array `M` is defined by the array `M.step[]`, so that the address of element , where , is computed as:  In case of a 2-dimensional array, the above formula is reduced to:  Note that `M.step[i] >= M.step[i+1]` (in fact, `M.step[i] >= M.step[i+1]*M.size[i+1]` ). This means that 2-dimensional matrices are stored row-by-row, 3-dimensional matrices are stored plane-by-plane, and so on. M.step[M.dims-1] is minimal and always equal to the element size M.elemSize() .
 *
 *
 * So, the data layout in Mat is compatible with the majority of dense array types from the standard toolkits and SDKs, such as Numpy (ndarray), Win32 (independent device bitmaps), and others, that is, with any array that uses *steps* (or *strides*) to compute the position of a pixel. Due to this compatibility, it is possible to make a Mat header for user-allocated data and process it in-place using OpenCV functions.
 *
 *
 * There are many different ways to create a Mat object. The most popular options are listed below:
 *
 *
 * - Use the create(nrows, ncols, type) method or the similar Mat(nrows, ncols, type[, fillValue]) constructor. A new array of the specified size and type is allocated. type has the same meaning as in the cvCreateMat method. For example, CV_8UC1 means a 8-bit single-channel array, CV_32FC2 means a 2-channel (complex) floating-point array, and so on. //makea7x7complexmatrixfilledwith1+3j.
 * MatM(7,7,CV_32FC2,Scalar(1,3));
 * //andnowturnMtoa100x6015-channel8-bitmatrix.
 * //Theoldcontentwillbedeallocated
 * M.create(100,60,CV_8UC(15));
 *  As noted in the introduction to this chapter, create() allocates only a new array when the shape or type of the current array are different from the specified ones.
 * - Create a multi-dimensional array: //createa100x100x1008-bitarray
 * intsz[]={100,100,100};
 * MatbigCube(3,sz,CV_8U,Scalar::all(0));
 *  It passes the number of dimensions =1 to the Mat constructor but the created array will be 2-dimensional with the number of columns set to 1. So, Mat::dims is always >= 2 (can also be 0 when the array is empty).
 * - Use a copy constructor or assignment operator where there can be an array or expression on the right side (see below). As noted in the introduction, the array assignment is an O(1) operation because it only copies the header and increases the reference counter. The Mat::clone() method can be used to get a full (deep) copy of the array when you need it.
 * - Construct a header for a part of another array. It can be a single row, single column, several rows, several columns, rectangular region in the array (called a *minor* in algebra) or a diagonal. Such operations are also O(1) because the new header references the same data. You can actually modify a part of the array using this feature, for example: //addthe5-throw,multipliedby3tothe3rdrow
 * M.row(3)=M.row(3)+M.row(5)*3;
 * //nowcopythe7-thcolumntothe1-stcolumn
 * //M.col(1)=M.col(7);//thiswillnotwork
 * MatM1=M.col(1);
 * M.col(7).copyTo(M1);
 * //createanew320x240image
 * Matimg(Size(320,240),CV_8UC3);
 * //selectaROI
 * Matroi(img,Rect(10,10,100,100));
 * //filltheROIwith(0,255,0)(whichisgreeninRGBspace);
 * //theoriginal320x240imagewillbemodified
 * roi=Scalar(0,255,0);
 *  Due to the additional datastart and dataend members, it is possible to compute a relative sub-array position in the main *container* array using locateROI(): MatA=Mat::eye(10,10,CV_32S);
 * //extractsAcolumns,1(inclusive)to3(exclusive).
 * MatB=A(Range::all(),Range(1,3));
 * //extractsBrows,5(inclusive)to9(exclusive).
 * //thatis,C\~A(Range(5,9),Range(1,3))
 * MatC=B(Range(5,9),Range::all());
 * Sizesize;Pointofs;
 * C.locateROI(size,ofs);
 * //sizewillbe(width=10,height=10)andtheofswillbe(x=1,y=5)
 *  As in case of whole matrices, if you need a deep copy, use the `clone()` method of the extracted sub-matrices.
 * - Make a header for user-allocated data. It can be useful to do the following:
 * - Process "foreign" data using OpenCV (for example, when you implement a DirectShow* filter or a processing module for gstreamer, and so on). For example: Matprocess_video_frame(constunsignedchar*pixels,
 * intwidth,intheight,intstep)
 * {
 * //wrapinputbuffer
 * Matimg(height,width,CV_8UC3,(unsignedchar*)pixels,step);
 *
 * Matresult;
 * GaussianBlur(img,result,Size(7,7),1.5,1.5);
 *
 * returnresult;
 * }
 * - Quickly initialize small matrices and/or get a super-fast element access. doublem[3][3]={{a,b,c},{d,e,f},{g,h,i}};
 * MatM=Mat(3,3,CV_64F,m).inv();
 * - Use MATLAB-style array initializers, zeros(), ones(), eye(), for example: //createadouble-precisionidentitymatrixandaddittoM.
 * M+=Mat::eye(M.rows,M.cols,CV_64F);
 * - Use a comma-separated initializer: //createa3x3double-precisionidentitymatrix
 * MatM=(Mat_<double>(3,3)<<1,0,0,0,1,0,0,0,1);
 *  With this approach, you first call a constructor of the Mat class with the proper parameters, and then you just put `<< operator` followed by comma-separated values that can be constants, variables, expressions, and so on. Also, note the extra parentheses required to avoid compilation errors.
 *
 *
 * Once the array is created, it is automatically managed via a reference-counting mechanism. If the array header is built on top of user-allocated data, you should handle the data by yourself. The array data is deallocated when no one points to it. If you want to release the data pointed by a array header before the array destructor is called, use Mat::release().
 *
 *
 * The next important thing to learn about the array class is element access. This manual already described how to compute an address of each array element. Normally, you are not required to use the formula directly in the code. If you know the array element type (which can be retrieved using the method Mat::type() ), you can access the element  of a 2-dimensional array as: M.at<double>(i,j)+=1.f;
 *  assuming that `M` is a double-precision floating-point array. There are several variants of the method at for a different number of dimensions.
 *
 *
 * If you need to process a whole row of a 2D array, the most efficient way is to get the pointer to the row first, and then just use the plain C operator [] : //computesumofpositivematrixelements
 * //(assumingthatMisadouble-precisionmatrix)
 * doublesum=0;
 * for(inti=0;i<M.rows;i++)
 * {
 * constdouble*Mi=M.ptr<double>(i);
 * for(intj=0;j<M.cols;j++)
 * sum+=std::max(Mi[j],0.);
 * }
 *  Some operations, like the one above, do not actually depend on the array shape. They just process elements of an array one by one (or elements from multiple arrays that have the same coordinates, for example, array addition). Such operations are called *element-wise*. It makes sense to check whether all the input/output arrays are continuous, namely, have no gaps at the end of each row. If yes, process them as a long single row: //computethesumofpositivematrixelements,optimizedvariant
 * doublesum=0;
 * intcols=M.cols,rows=M.rows;
 * if(M.isContinuous())
 * {
 * cols*=rows;
 * rows=1;
 * }
 * for(inti=0;i<rows;i++)
 * {
 * constdouble*Mi=M.ptr<double>(i);
 * for(intj=0;j<cols;j++)
 * sum+=std::max(Mi[j],0.);
 * }
 *  In case of the continuous matrix, the outer loop body is executed just once. So, the overhead is smaller, which is especially noticeable in case of small matrices.
 *
 *
 * Finally, there are STL-style iterators that are smart enough to skip gaps between successive rows: //computesumofpositivematrixelements,iterator-basedvariant
 * doublesum=0;
 * MatConstIterator_<double>it=M.begin<double>(),it_end=M.end<double>();
 * for(;it!=it_end;++it)
 * sum+=std::max(*it,0.);
 *  The matrix iterators are random-access iterators, so they can be passed to any STL algorithm, including std::sort().
 *
 *
 * @note Matrix Expressions and arithmetic see MatExpr
 */
export declare class Mat extends Mat_ {
/**
 * custom allocator
 */
  public allocator: MatAllocator

  public cols: number

  public dataend: number

  public datalimit: number

/**
 * helper fields used in locateROI and adjustROI
 */
  public datastart: number

/**
 * the matrix dimensionality, >= 2
 */
  public dims: number

  public dummy: number

/**
 * includes several bit-fields:
 * - the magic signature
 * - continuity flag
 * - depth
 * - number of channels
 */
  public flags: number

/**
 * the number of rows and columns or (-1, -1) when the matrix has more than 2 dimensions
 */
  public rows: number

  public step: MatStep

/**
 * interaction with UMat
 */
  public u: UMatData

/**
 * These are various constructors that form a matrix. As noted in the AutomaticAllocation, often the default constructor is enough, and the proper matrix will be allocated by an OpenCV function. The constructed matrix can further be assigned to another matrix or matrix expression or can be allocated with Mat::create . In the former case, the old content is de-referenced.
 */
  public constructor()

/**
 * download data from GpuMat
 */
  public constructor(m: any)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param m Array that (as a whole or partly) is assigned to the constructed matrix. No data is copied by these constructors. Instead, the header pointing to m data or its sub-array is constructed and associated with it. The reference counter, if any, is incremented. So, when you modify the matrix formed using such a constructor, you also modify the corresponding elements of m . If you want to have an independent copy of the sub-array, use Mat::clone() .
 */
  public constructor(m: any)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param m Array that (as a whole or partly) is assigned to the constructed matrix. No data is copied by these constructors. Instead, the header pointing to m data or its sub-array is constructed and associated with it. The reference counter, if any, is incremented. So, when you modify the matrix formed using such a constructor, you also modify the corresponding elements of m . If you want to have an independent copy of the sub-array, use Mat::clone() .
 * @param rowRange Range of the m rows to take. As usual, the range start is inclusive and the range end is exclusive. Use Range::all() to take all the rows.
 * @param colRange Range of the m columns to take. Use Range::all() to take all the columns.
 */
  public constructor(m: any, rowRange: Range, colRange?: Range)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param m Array that (as a whole or partly) is assigned to the constructed matrix. No data is copied by these constructors. Instead, the header pointing to m data or its sub-array is constructed and associated with it. The reference counter, if any, is incremented. So, when you modify the matrix formed using such a constructor, you also modify the corresponding elements of m . If you want to have an independent copy of the sub-array, use Mat::clone() .
 * @param ranges Array of selected ranges of m along each dimensionality.
 */
  public constructor(m: any, ranges: Range)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param m Array that (as a whole or partly) is assigned to the constructed matrix. No data is copied by these constructors. Instead, the header pointing to m data or its sub-array is constructed and associated with it. The reference counter, if any, is incremented. So, when you modify the matrix formed using such a constructor, you also modify the corresponding elements of m . If you want to have an independent copy of the sub-array, use Mat::clone() .
 * @param roi Region of interest.
 */
  public constructor(m: any, roi: Rect)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param m Array that (as a whole or partly) is assigned to the constructed matrix. No data is copied by these constructors. Instead, the header pointing to m data or its sub-array is constructed and associated with it. The reference counter, if any, is incremented. So, when you modify the matrix formed using such a constructor, you also modify the corresponding elements of m . If you want to have an independent copy of the sub-array, use Mat::clone() .
 * @param ranges Array of selected ranges of m along each dimensionality.
 */
  public constructor(m: any, ranges: Range)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public constructor(commaInitializer: MatCommaInitializer_)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param shape Array shape.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 */
  public constructor(shape: MatShape, type: number)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param shape Array shape.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 * @param s An optional value to initialize each matrix element with. To set all the matrix elements to the particular value after the construction, use the assignment operator Mat::operator=(const Scalar& value) .
 */
  public constructor(shape: MatShape, type: number, s: Scalar)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param shape Array shape.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 * @param data Pointer to the user data. Matrix constructors that take data and step parameters do not allocate matrix data. Instead, they just initialize the matrix header that points to the specified data, which means that no data is copied. This operation is very efficient and can be used to process external data using OpenCV functions. The external data is not automatically deallocated, so you should take care of it.
 * @param steps Array of ndims-1 steps in case of a multi-dimensional array (the last step is always set to the element size). If not specified, the matrix is assumed to be continuous.
 */
  public constructor(shape: MatShape, type: number, data: any, steps?: any)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public constructor(mtx: Matx, copyData?: boolean)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public constructor(pt: Point3_, copyData?: boolean)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public constructor(pt: Point_, copyData?: boolean)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public constructor(arr: _Tp, copyData?: boolean)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public constructor(list: _Tp)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public constructor(sizes: any, list: _Tp)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param vec STL vector whose elements form the matrix. The matrix has a single column and the number of rows equal to the number of vector elements. Type of the matrix matches the type of vector elements. The constructor can handle arbitrary types, for which there is a properly declared DataType . This means that the vector elements must be primitive numbers or uni-type numerical tuples of numbers. Mixed-type structures are not supported. The corresponding constructor is explicit. Since STL vectors are not automatically converted to Mat instances, you should write Mat(vec) explicitly. Unless you copy the data into the matrix ( copyData=true ), no new elements will be added to the vector because it can potentially yield vector data reallocation, and, thus, the matrix data pointer will be invalid.
 * @param copyData Flag to specify whether the underlying data of the STL vector should be copied to (true) or shared with (false) the newly constructed matrix. When the data is copied, the allocated buffer is managed using Mat reference counting mechanism. While the data is shared, the reference counter is NULL, and you should not deallocate the data until the matrix is destructed.
 */
  public constructor(vec: _Tp, copyData?: boolean)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param sizes Array of integers specifying an n-dimensional array shape.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 */
  public constructor(sizes: any, type: number)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param sizes Array of integers specifying an n-dimensional array shape.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 * @param s An optional value to initialize each matrix element with. To set all the matrix elements to the particular value after the construction, use the assignment operator Mat::operator=(const Scalar& value) .
 */
  public constructor(sizes: any, type: number, s: Scalar)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param sizes Array of integers specifying an n-dimensional array shape.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 * @param data Pointer to the user data. Matrix constructors that take data and step parameters do not allocate matrix data. Instead, they just initialize the matrix header that points to the specified data, which means that no data is copied. This operation is very efficient and can be used to process external data using OpenCV functions. The external data is not automatically deallocated, so you should take care of it.
 * @param steps Array of ndims-1 steps in case of a multi-dimensional array (the last step is always set to the element size). If not specified, the matrix is assumed to be continuous.
 */
  public constructor(sizes: any, type: number, data: any, steps?: any)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public constructor(vec: Vec, copyData?: boolean)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param ndims Array dimensionality.
 * @param sizes Array of integers specifying an n-dimensional array shape.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 */
  public constructor(ndims: number, sizes: any, type: number)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param ndims Array dimensionality.
 * @param sizes Array of integers specifying an n-dimensional array shape.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 * @param s An optional value to initialize each matrix element with. To set all the matrix elements to the particular value after the construction, use the assignment operator Mat::operator=(const Scalar& value) .
 */
  public constructor(ndims: number, sizes: any, type: number, s: Scalar)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param ndims Array dimensionality.
 * @param sizes Array of integers specifying an n-dimensional array shape.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 * @param data Pointer to the user data. Matrix constructors that take data and step parameters do not allocate matrix data. Instead, they just initialize the matrix header that points to the specified data, which means that no data is copied. This operation is very efficient and can be used to process external data using OpenCV functions. The external data is not automatically deallocated, so you should take care of it.
 * @param steps Array of ndims-1 steps in case of a multi-dimensional array (the last step is always set to the element size). If not specified, the matrix is assumed to be continuous.
 */
  public constructor(ndims: number, sizes: any, type: number, data: any, steps?: any)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param rows Number of rows in a 2D array.
 * @param cols Number of columns in a 2D array.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 */
  public constructor(rows: number, cols: number, type: number)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param rows Number of rows in a 2D array.
 * @param cols Number of columns in a 2D array.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 * @param s An optional value to initialize each matrix element with. To set all the matrix elements to the particular value after the construction, use the assignment operator Mat::operator=(const Scalar& value) .
 */
  public constructor(rows: number, cols: number, type: number, s: Scalar)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param rows Number of rows in a 2D array.
 * @param cols Number of columns in a 2D array.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 * @param data Pointer to the user data. Matrix constructors that take data and step parameters do not allocate matrix data. Instead, they just initialize the matrix header that points to the specified data, which means that no data is copied. This operation is very efficient and can be used to process external data using OpenCV functions. The external data is not automatically deallocated, so you should take care of it.
 * @param step Number of bytes each matrix row occupies. The value should include the padding bytes at the end of each row, if any. If the parameter is missing (set to AUTO_STEP ), no padding is assumed and the actual step is calculated as cols*elemSize(). See Mat::elemSize.
 */
  public constructor(rows: number, cols: number, type: number, data: any, step?: number)

  public constructor(m: any)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param size 2D array size: Size(cols, rows) . In the Size() constructor, the number of rows and the number of columns go in the reverse order.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 */
  public constructor(size: Size, type: number)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param size 2D array size: Size(cols, rows) . In the Size() constructor, the number of rows and the number of columns go in the reverse order.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 * @param s An optional value to initialize each matrix element with. To set all the matrix elements to the particular value after the construction, use the assignment operator Mat::operator=(const Scalar& value) .
 */
  public constructor(size: Size, type: number, s: Scalar)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param size 2D array size: Size(cols, rows) . In the Size() constructor, the number of rows and the number of columns go in the reverse order.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 * @param data Pointer to the user data. Matrix constructors that take data and step parameters do not allocate matrix data. Instead, they just initialize the matrix header that points to the specified data, which means that no data is copied. This operation is very efficient and can be used to process external data using OpenCV functions. The external data is not automatically deallocated, so you should take care of it.
 * @param step Number of bytes each matrix row occupies. The value should include the padding bytes at the end of each row, if any. If the parameter is missing (set to AUTO_STEP ), no padding is assumed and the actual step is calculated as cols*elemSize(). See Mat::elemSize.
 */
  public constructor(size: Size, type: number, data: any, step?: number)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param shape Array shape.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 */
  public constructor(shape: any, type: number)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param shape Array shape.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 * @param s An optional value to initialize each matrix element with. To set all the matrix elements to the particular value after the construction, use the assignment operator Mat::operator=(const Scalar& value) .
 */
  public constructor(shape: any, type: number, s: Scalar)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param shape Array shape.
 * @param type Array type. Use CV_8UC1, ..., CV_64FC4 to create 1-4 channel matrices, or CV_8UC(n), ..., CV_64FC(n) to create multi-channel (up to CV_CN_MAX channels) matrices.
 * @param data Pointer to the user data. Matrix constructors that take data and step parameters do not allocate matrix data. Instead, they just initialize the matrix header that points to the specified data, which means that no data is copied. This operation is very efficient and can be used to process external data using OpenCV functions. The external data is not automatically deallocated, so you should take care of it.
 * @param steps Array of ndims-1 steps in case of a multi-dimensional array (the last step is always set to the element size). If not specified, the matrix is assumed to be continuous.
 */
  public constructor(shape: any, type: number, data: any, steps?: any)

/**
 * Increments the reference counter.
 *
 * The method increments the reference counter associated with the matrix data. If the matrix header points to an external data set (see Mat::Mat ), the reference counter is NULL, and the method has no effect in this case. Normally, to avoid memory leaks, the method should not be called explicitly. It is called implicitly by the matrix assignment operator. The reference counter increment is an atomic operation on the platforms that support it. Thus, it is safe to operate on the same matrices asynchronously in different threads.
 */
  public addref(): void

/**
 * Adjusts a submatrix size and position within the parent matrix.
 *
 * The method is complimentary to Mat::locateROI . The typical use of these functions is to determine the submatrix position within the parent matrix and then shift the position somehow. Typically, it can be required for filtering operations when pixels outside of the ROI should be taken into account. When all the method parameters are positive, the ROI needs to grow in all directions by the specified amount, for example: A.adjustROI(2,2,2,2);
 *  In this example, the matrix size is increased by 4 elements in each direction. The matrix is shifted by 2 elements to the left and 2 elements up, which brings in all the necessary pixels for the filtering with the 5x5 kernel.
 *
 *
 * adjustROI forces the adjusted ROI to be inside of the parent matrix that is boundaries of the adjusted ROI are constrained by boundaries of the parent matrix. For example, if the submatrix A is located in the first row of a parent matrix and you called A.adjustROI(2, 2, 2, 2) then A will not be increased in the upward direction.
 *
 *
 * The function is used internally by the OpenCV filtering functions, like filter2D , morphological operations, and so on.
 * @see copyMakeBorder
 *
 * @param dtop Shift of the top submatrix boundary upwards.
 * @param dbottom Shift of the bottom submatrix boundary downwards.
 * @param dleft Shift of the left submatrix boundary to the left.
 * @param dright Shift of the right submatrix boundary to the right.
 */
  public adjustROI(dtop: number, dbottom: number, dleft: number, dright: number): Mat

/**
 * Provides a functional form of convertTo.
 *
 * This is an internally used method called by the MatrixExpressions engine.
 *
 * @param m Destination array.
 * @param type Desired destination array depth (or -1 if it should be the same as the source type).
 */
  public assignTo(m: Mat, type?: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param idx Array of Mat::dims indices.
 */
  public at(idx: any): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param idx Array of Mat::dims indices.
 */
  public at(idx: any): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public at(idx: Vec): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public at(idx: Vec): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param i0 Index along the dimension 0
 * @param i1 Index along the dimension 1
 * @param i2 Index along the dimension 2
 */
  public at(i0: number, i1: number, i2: number): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param i0 Index along the dimension 0
 * @param i1 Index along the dimension 1
 * @param i2 Index along the dimension 2
 */
  public at(i0: number, i1: number, i2: number): _Tp

/**
 * Returns a reference to the specified array element.
 *
 * The template methods return a reference to the specified array element. For the sake of higher performance, the index range checks are only performed in the Debug configuration.
 *
 *
 * Note that the variants with a single index (i) can be used to access elements of single-row or single-column 2-dimensional arrays. That is, if, for example, A is a 1 x N floating-point matrix and B is an M x 1 integer matrix, you can simply write `A.at<float>(k+4)` and `B.at<int>(2*i+1)` instead of `A.at<float>(0,k+4)` and `B.at<int>(2*i+1,0)`, respectively.
 *
 *
 * The example below initializes a Hilbert matrix: MatH(100,100,CV_64F);
 * for(inti=0;i<H.rows;i++)
 * for(intj=0;j<H.cols;j++)
 * H.at<double>(i,j)=1./(i+j+1);
 *
 *
 * Keep in mind that the size identifier used in the at operator cannot be chosen at random. It depends on the image from which you are trying to retrieve the data. The table below gives a better insight in this:
 * - If matrix is of type `CV_8U` then use `Mat.at<uchar>(y,x)`.
 * - If matrix is of type `CV_8S` then use `Mat.at<schar>(y,x)`.
 * - If matrix is of type `CV_16U` then use `Mat.at<ushort>(y,x)`.
 * - If matrix is of type `CV_16S` then use `Mat.at<short>(y,x)`.
 * - If matrix is of type `CV_32S` then use `Mat.at<int>(y,x)`.
 * - If matrix is of type `CV_32F` then use `Mat.at<float>(y,x)`.
 * - If matrix is of type `CV_64F` then use `Mat.at<double>(y,x)`.
 *
 * @param i0 Index along the dimension 0
 */
  public at(i0?: number): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param i0 Index along the dimension 0
 */
  public at(i0?: number): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param row Index along the dimension 0
 * @param col Index along the dimension 1
 */
  public at(row: number, col: number): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param row Index along the dimension 0
 * @param col Index along the dimension 1
 */
  public at(row: number, col: number): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts. special versions for 2D arrays (especially convenient for referencing image pixels)
 *
 * @param pt Element position specified as Point(j,i) .
 */
  public at(pt: Point): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts. special versions for 2D arrays (especially convenient for referencing image pixels)
 *
 * @param pt Element position specified as Point(j,i) .
 */
  public at(pt: Point): _Tp

/**
 * Returns the matrix iterator and sets it to the first matrix element.
 *
 * The methods return the matrix read-only or read-write iterators. The use of matrix iterators is very similar to the use of bi-directional STL iterators. In the example below, the alpha blending function is rewritten using the matrix iterators: template<typenameT>
 * voidalphaBlendRGBA(constMat&src1,constMat&src2,Mat&dst)
 * {
 * typedefVec<T,4>VT;
 *
 * constfloatalpha_scale=(float)std::numeric_limits<T>::max(),
 * inv_scale=1.f/alpha_scale;
 *
 * CV_Assert(src1.type()==src2.type()&&
 * src1.type()==traits::Type<VT>::value&&
 * src1.size()==src2.size());
 * Sizesize=src1.size();
 * dst.create(size,src1.type());
 *
 * MatConstIterator_<VT>it1=src1.begin<VT>(),it1_end=src1.end<VT>();
 * MatConstIterator_<VT>it2=src2.begin<VT>();
 * MatIterator_<VT>dst_it=dst.begin<VT>();
 *
 * for(;it1!=it1_end;++it1,++it2,++dst_it)
 * {
 * VTpix1=*it1,pix2=*it2;
 * floatalpha=pix1[3]*inv_scale,beta=pix2[3]*inv_scale;
 * *dst_it=VT(saturate_cast<T>(pix1[0]*alpha+pix2[0]*beta),
 * saturate_cast<T>(pix1[1]*alpha+pix2[1]*beta),
 * saturate_cast<T>(pix1[2]*alpha+pix2[2]*beta),
 * saturate_cast<T>((1-(1-alpha)*(1-beta))*alpha_scale));
 * }
 * }
 */
  public begin(): MatIterator_

  public begin(): MatConstIterator_

/**
 * Returns the number of matrix channels.
 *
 * The method returns the number of matrix channels.
 */
  public channels(): number

/**
 * @return -1 if the requirement is not satisfied. Otherwise, it returns the number of elements in the matrix. Note that an element may have multiple channels.
 *
 * The following code demonstrates its usage for a 2-d matrix: cv::Matmat(20,1,CV_32FC2);
 * intn=mat.checkVector(2);
 * CV_Assert(n==20);//mathas20elements
 *
 * mat.create(20,2,CV_32FC1);
 * n=mat.checkVector(1);
 * CV_Assert(n==-1);//matisneitheracolumnnorarowvector
 *
 * n=mat.checkVector(2);
 * CV_Assert(n==20);//the2columnsareconsideredas1element
 *
 *
 * The following code demonstrates its usage for a 3-d matrix: intdims[]={1,3,5};//1plane,everyplanehas3rowsand5columns
 * mat.create(3,dims,CV_32FC1);//for3-dmat,itMUSThaveonly1channel
 * n=mat.checkVector(5);//the5columnsareconsideredas1element
 * CV_Assert(n==3);
 *
 * intdims2[]={3,1,5};//3planes,everyplanehas1rowand5columns
 * mat.create(3,dims2,CV_32FC1);
 * n=mat.checkVector(5);//the5columnsareconsideredas1element
 * CV_Assert(n==3);
 *
 * @param elemChannels Number of channels or number of columns the matrix should have. For a 2-D matrix, when the matrix has only 1 column, then it should have elemChannels channels; When the matrix has only 1 channel, then it should have elemChannels columns. For a 3-D matrix, it should have only one channel. Furthermore, if the number of planes is not one, then the number of rows within every plane has to be 1; if the number of rows within every plane is not 1, then the number of planes has to be 1.
 * @param depth The depth the matrix should have. Set it to -1 when any depth is fine.
 * @param requireContinuous Set it to true to require the matrix to be continuous
 */
  public checkVector(elemChannels: number, depth?: number, requireContinuous?: boolean): number

/**
 * Creates a matrix header for the specified matrix column.
 *
 * The method makes a new header for the specified matrix column and returns it. This is an O(1) operation, regardless of the matrix size. The underlying data of the new matrix is shared with the original matrix. See also the Mat::row description.
 *
 * @param x A 0-based column index.
 */
  public col(x: number): Mat

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param r Range structure containing both the start and the end indices.
 */
  public colRange(r: Range): Mat

/**
 * Creates a matrix header for the specified column span.
 *
 * The method makes a new header for the specified column span of the matrix. Similarly to Mat::row and Mat::col , this is an O(1) operation.
 *
 * @param startcol An inclusive 0-based start index of the column span.
 * @param endcol An exclusive 0-based ending index of the column span.
 */
  public colRange(startcol: number, endcol: number): Mat

/**
 * Converts an array to another data type with optional scaling.
 *
 * The method converts source pixel values to the target data type. saturate_cast<> is applied at the end to avoid possible overflows:
 *
 * @param m output matrix; if it does not have a proper size or type before the operation, it is reallocated.
 * @param rtype desired output matrix type or, rather, the depth since the number of channels are the same as the input has; if rtype is negative, the output matrix will have the same type as the input.
 * @param alpha optional scale factor.
 * @param beta optional delta added to the scaled values.
 */
  public convertTo(m: OutputArray, rtype: number, alpha?: number, beta?: number): void

/**
 * Overwrites the existing matrix.
 *
 * This method writes existing matrix data, just like copyTo(). But if it does not have a proper size or type before the operation, an exception is thrown. This function is helpful to update ROI in an existing matrix.
 *
 *
 * If (re)allocation of destination memory is necessary, use copyTo() .
 *
 *
 * @see copyTo
 *
 * @param m Destination matrix. If it does not have a proper size or type before the operation, an exception is thrown.
 */
  public copyAt(m: OutputArray): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param m Destination matrix. If it does not have a proper size or type before the operation, an exception is thrown.
 * @param mask Operation mask of the same size as *this. Its non-zero elements indicate which matrix elements need to be copied. The mask has to be of type CV_8U and can have 1 or multiple channels.
 */
  public copyAt(m: OutputArray, mask: InputArray): void

/**
 * internal use function; properly re-allocates _size, _step arrays
 */
  public copySize(m: Mat): void

/**
 * Copies the matrix to another one.
 *
 * The method copies the matrix data to another matrix. Before copying the data, the method invokes : m.create(this->size(),this->type());
 *  so that the destination matrix is reallocated if needed. While m.copyTo(m); works flawlessly, the function does not handle the case of a partial overlap between the source and the destination matrices.
 *
 *
 * When the operation mask is specified, if the Mat::create call shown above reallocates the matrix, the newly allocated matrix is initialized with all zeros before copying the data.
 *
 *
 * If (re)allocation of destination memory is not necessary (e.g. updating ROI), use copyAt() .
 *
 *
 * @see copyAt
 *
 * @param m Destination matrix. If it does not have a proper size or type before the operation, it is reallocated.
 */
  public copyTo(m: OutputArray): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param m Destination matrix. If it does not have a proper size or type before the operation, it is reallocated.
 * @param mask Operation mask of the same size as *this. Its non-zero elements indicate which matrix elements need to be copied. The mask has to be of type CV_8U, CV_8S or CV_Bool and can have 1 or multiple channels.
 */
  public copyTo(m: OutputArray, mask: InputArray): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param shape The new shape.
 * @param type New matrix type.
 */
  public create(shape: MatShape, type: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param sizes Array of integers specifying a new array shape.
 * @param type New matrix type.
 */
  public create(sizes: any, type: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param ndims New array dimensionality.
 * @param sizes Array of integers specifying a new array shape.
 * @param type New matrix type.
 */
  public create(ndims: number, sizes: any, type: number): void

/**
 * Allocates new array data if needed.
 *
 * This is one of the key Mat methods. Most new-style OpenCV functions and methods that produce arrays call this method for each output array. The method uses the following algorithm:
 *
 *
 * - If the current array shape and the type match the new ones, return immediately. Otherwise, de-reference the previous data by calling Mat::release.
 * - Initialize the new header.
 * - Allocate the new data of total()*elemSize() bytes.
 * - Allocate the new, associated with the data, reference counter and set it to 1.
 *
 *
 * Such a scheme makes the memory management robust and efficient at the same time and helps avoid extra typing for you. This means that usually there is no need to explicitly allocate output arrays. That is, instead of writing: Matcolor;
 * ...
 * Matgray(color.rows,color.cols,color.depth());
 * cvtColor(color,gray,COLOR_BGR2GRAY);
 *  you can simply write: Matcolor;
 * ...
 * Matgray;
 * cvtColor(color,gray,COLOR_BGR2GRAY);
 *  because cvtColor, as well as the most of OpenCV functions, calls Mat::create() for the output array internally.
 *
 * @param rows New number of rows.
 * @param cols New number of columns.
 * @param type New matrix type.
 */
  public create(rows: number, cols: number, type: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param size Alternative new matrix size specification: Size(cols, rows)
 * @param type New matrix type.
 */
  public create(size: Size, type: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param shape The new shape.
 * @param type New matrix type.
 */
  public create(shape: any, type: number): void

/**
 * Creates the matrix of the same size as another array.
 *
 * The method is similar to _OutputArray::createSameSize(arr, type), but is applied to Mat.
 *
 * @param arr The other array.
 * @param type New matrix type.
 */
  public createSameSize(arr: InputArray, type: number): void

/**
 * Computes a cross-product of two 3-element vectors.
 *
 * The method computes a cross-product of two 3-element vectors. The vectors must be 3-element floating-point vectors of the same shape and size. The result is another 3-element vector of the same shape and type as operands.
 *
 * @param m Another cross-product operand.
 */
  public cross(m: InputArray): Mat

/**
 * internal use function, consider to use 'release' method instead; deallocates the matrix data
 */
  public deallocate(): void

/**
 * Returns the depth of a matrix element.
 *
 * The method returns the identifier of the matrix element depth (the type of each individual channel). For example, for a 16-bit signed element array, the method returns CV_16S . A complete list of matrix types contains the following values:
 * - CV_8U - 8-bit unsigned integers ( 0..255 )
 * - CV_8S - 8-bit signed integers ( -128..127 )
 * - CV_16U - 16-bit unsigned integers ( 0..65535 )
 * - CV_16S - 16-bit signed integers ( -32768..32767 )
 * - CV_32S - 32-bit signed integers ( -2147483648..2147483647 )
 * - CV_32F - 32-bit floating-point numbers ( -FLT_MAX..FLT_MAX, INF, NAN )
 * - CV_64F - 64-bit floating-point numbers ( -DBL_MAX..DBL_MAX, INF, NAN )
 */
  public depth(): number

/**
 * Extracts a diagonal from a matrix.
 *
 * The method makes a new header for the specified matrix diagonal. The new matrix is represented as a single-column matrix. Similarly to Mat::row and Mat::col, this is an O(1) operation.
 *
 * @param d index of the diagonal, with the following values: - `d=0` is the main diagonal. - `d<0` is a diagonal from the lower half. For example, d=-1 means the diagonal is set immediately below the main one. - `d>0` is a diagonal from the upper half. For example, d=1 means the diagonal is set immediately above the main one. For example: Matm=(Mat_<int>(3,3)<< 1,2,3, 4,5,6, 7,8,9); Matd0=m.diag(0); Matd1=m.diag(1); Matd_1=m.diag(-1);  The resulting matrices are d0= [1; 5; 9] d1= [2; 6] d_1= [4; 8]
 */
  public diag(d?: number): Mat

/**
 * Computes a dot-product of two vectors.
 *
 * The method computes a dot-product of two matrices. If the matrices are not single-column or single-row vectors, the top-to-bottom left-to-right scan ordering is used to treat them as 1D vectors. The vectors must have the same size and type. If the matrices have more than one channel, the dot products from all the channels are summed together.
 *
 * @param m another dot-product operand.
 */
  public dot(m: InputArray): number

/**
 * Returns the matrix element size in bytes.
 *
 * The method returns the matrix element size in bytes. For example, if the matrix type is CV_16SC3 , the method returns 3*sizeof(short) or 6.
 */
  public elemSize(): number

/**
 * Returns the size of each matrix element channel in bytes.
 *
 * The method returns the matrix element channel size in bytes, that is, it ignores the number of channels. For example, if the matrix type is CV_16SC3 , the method returns sizeof(short) or 2.
 */
  public elemSize1(): number

/**
 * Returns true if the array has no elements.
 *
 * The method returns true if Mat::total() is 0 or if Mat::data is NULL. Because of pop_back() and resize() methods `M.total() == 0` does not imply that `M.data == NULL`.
 */
  public empty(): boolean

/**
 * Returns the matrix iterator and sets it to the after-last matrix element.
 *
 * The methods return the matrix read-only or read-write iterators, set to the point following the last matrix element.
 */
  public end(): MatIterator_

  public end(): MatConstIterator_

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param shape The new shape.
 * @param type New matrix type.
 */
  public fit(shape: MatShape, type: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param sizes Array of integers specifying a new array shape.
 * @param type New matrix type.
 */
  public fit(sizes: any, type: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param ndims New array dimensionality.
 * @param sizes Array of integers specifying a new array shape.
 * @param type New matrix type.
 */
  public fit(ndims: number, sizes: any, type: number): void

/**
 * Similar to create(rows, cols, type), but only reallocates memory if the existing buffer size is not enough.
 *
 * @param rows New number of rows.
 * @param cols New number of columns.
 * @param type New matrix type.
 */
  public fit(rows: number, cols: number, type: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param size Alternative new matrix size specification: Size(cols, rows)
 * @param type New matrix type.
 */
  public fit(size: Size, type: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param shape The new shape.
 * @param type New matrix type.
 */
  public fit(shape: any, type: number): void

/**
 * Similar to createSameSize(arr, type), but only reallocates memory if the existing buffer is not enough.
 *
 * @param arr The other array.
 * @param type New matrix type.
 */
  public fitSameSize(arr: InputArray, type: number): void

/**
 * Runs the given functor over all matrix elements in parallel.
 *
 * The operation passed as argument has to be a function pointer, a function object or a lambda(C++11).
 *
 *
 * Example 1. All of the operations below put 0xFF the first channel of all matrix elements: Matimage(1920,1080,CV_8UC3);
 * typedefcv::Point3_<uint8_t>Pixel;
 *
 * //first.rawpointeraccess.
 * for(intr=0;r<image.rows;++r){
 * Pixel*ptr=image.ptr<Pixel>(r,0);
 * constPixel*ptr_end=ptr+image.cols;
 * for(;ptr!=ptr_end;++ptr){
 * ptr->x=255;
 * }
 * }
 *
 * //UsingMatIterator.(SimplebutthereareaIterator'soverhead)
 * for(Pixel&p:cv::Mat_<Pixel>(image)){
 * p.x=255;
 * }
 *
 * //Parallelexecutionwithfunctionobject.
 * structOperator{
 * voidoperator()(Pixel&pixel,constint*position){
 * pixel.x=255;
 * }
 * };
 * image.forEach<Pixel>(Operator());
 *
 * //ParallelexecutionusingC++11lambda.
 * image.forEach<Pixel>([](Pixel&p,constint*position)->void{
 * p.x=255;
 * });
 *  Example 2. Using the pixel's position: //Creating3Dmatrix(255x255x255)typeduint8_t
 * //andinitializeallelementsbythevaluewhichequalselementsposition.
 * //i.e.pixels(x,y,z)=(1,2,3)is(b,g,r)=(1,2,3).
 *
 * intsizes[]={255,255,255};
 * typedefcv::Point3_<uint8_t>Pixel;
 *
 * Mat_<Pixel>image=Mat::zeros(3,sizes,CV_8UC3);
 *
 * image.forEach<Pixel>([](Pixel&pixel,constintposition[])->void{
 * pixel.x=position[0];
 * pixel.y=position[1];
 * pixel.z=position[2];
 * });
 */
  public forEach(operation: any): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public forEach(operation: any): void

/**
 * retrieve UMat from Mat
 */
  public getUMat(accessFlags: AccessFlag, usageFlags?: UMatUsageFlags): UMat

/**
 * Inverses a matrix.
 *
 * The method performs a matrix inversion by means of matrix expressions. This means that a temporary matrix inversion object is returned by the method and can be used further as a part of more complex matrix expressions or can be assigned to a matrix.
 *
 * @param method Matrix inversion method. One of cv::DecompTypes
 */
  public inv(method?: number): MatExpr

/**
 * Reports whether the matrix is continuous or not.
 *
 * The method returns true if the matrix elements are stored continuously without gaps at the end of each row. Otherwise, it returns false. Obviously, 1x1 or 1xN matrices are always continuous. Matrices created with Mat::create are always continuous. But if you extract a part of the matrix using Mat::col, Mat::diag, and so on, or constructed a matrix header for externally allocated data, such matrices may no longer have this property.
 *
 *
 * The continuity flag is stored as a bit in the Mat::flags field and is computed automatically when you construct a matrix header. Thus, the continuity check is a very fast operation, though theoretically it could be done as follows: //alternativeimplementationofMat::isContinuous()
 * boolmyCheckMatContinuity(constMat&m)
 * {
 * //return(m.flags&Mat::CONTINUOUS_FLAG)!=0;
 * returnm.rows==1||m.step==m.cols*m.elemSize();
 * }
 *  The method is used in quite a few of OpenCV functions. The point is that element-wise operations (such as arithmetic and logical operations, math functions, alpha blending, color space transformations, and others) do not depend on the image geometry. Thus, if all the input and output arrays are continuous, the functions can process them as very long single-row vectors. The example below illustrates how an alpha-blending function can be implemented: template<typenameT>
 * voidalphaBlendRGBA(constMat&src1,constMat&src2,Mat&dst)
 * {
 * constfloatalpha_scale=(float)std::numeric_limits<T>::max(),
 * inv_scale=1.f/alpha_scale;
 *
 * CV_Assert(src1.type()==src2.type()&&
 * src1.type()==CV_MAKETYPE(traits::Depth<T>::value,4)&&
 * src1.size()==src2.size());
 * Sizesize=src1.size();
 * dst.create(size,src1.type());
 *
 * //hereistheidiom:checkthearraysforcontinuityand,
 * //ifthisisthecase,
 * //treatthearraysas1Dvectors
 * if(src1.isContinuous()&&src2.isContinuous()&&dst.isContinuous())
 * {
 * size.width*=size.height;
 * size.height=1;
 * }
 * size.width*=4;
 *
 * for(inti=0;i<size.height;i++)
 * {
 * //whenthearraysarecontinuous,
 * //theouterloopisexecutedonlyonce
 * constT*ptr1=src1.ptr<T>(i);
 * constT*ptr2=src2.ptr<T>(i);
 * T*dptr=dst.ptr<T>(i);
 *
 * for(intj=0;j<size.width;j+=4)
 * {
 * floatalpha=ptr1[j+3]*inv_scale,beta=ptr2[j+3]*inv_scale;
 * dptr[j]=saturate_cast<T>(ptr1[j]*alpha+ptr2[j]*beta);
 * dptr[j+1]=saturate_cast<T>(ptr1[j+1]*alpha+ptr2[j+1]*beta);
 * dptr[j+2]=saturate_cast<T>(ptr1[j+2]*alpha+ptr2[j+2]*beta);
 * dptr[j+3]=saturate_cast<T>((1-(1-alpha)*(1-beta))*alpha_scale);
 * }
 * }
 * }
 *  This approach, while being very simple, can boost the performance of a simple element-operation by 10-20 percents, especially if the image is rather small and the operation is quite simple.
 *
 *
 * Another OpenCV idiom in this function, a call of Mat::create for the destination array, that allocates the destination array unless it already has the proper size and type. And while the newly allocated arrays are always continuous, you still need to check the destination array because Mat::create does not always allocate a new matrix.
 */
  public isContinuous(): boolean

/**
 * returns true if the matrix is a submatrix of another matrix
 */
  public isSubmatrix(): boolean

/**
 * Locates the matrix header within a parent matrix.
 *
 * After you extracted a submatrix from a matrix using Mat::row, Mat::col, Mat::rowRange, Mat::colRange, and others, the resultant submatrix points just to the part of the original big matrix. However, each submatrix contains information (represented by datastart and dataend fields) that helps reconstruct the original matrix size and the position of the extracted submatrix within the original matrix. The method locateROI does exactly that.
 *
 * @param wholeSize Output parameter that contains the size of the whole matrix containing *this* as a part.
 * @param ofs Output parameter that contains an offset of *this* inside the whole matrix.
 */
  public locateROI(wholeSize: Size, ofs: Point): void

/**
 * Performs an element-wise multiplication or division of the two matrices.
 *
 * The method returns a temporary object encoding per-element array multiplication, with optional scale. Note that this is not a matrix multiplication that corresponds to a simpler "\*" operator.
 *
 *
 * Example: MatC=A.mul(5/B);//equivalenttodivide(A,B,C,5)
 *
 * @param m Another array of the same type and the same size as *this, or a matrix expression.
 * @param scale Optional scale factor.
 */
  public mul(m: InputArray, scale?: number): MatExpr

/**
 * Removes elements from the bottom of the matrix.
 *
 * The method removes one or more rows from the bottom of the matrix.
 *
 * @param nelems Number of removed rows. If it is greater than the total number of rows, an exception is thrown.
 */
  public pop_back(nelems?: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(idx: any): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(idx: any): number

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(idx: any): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(idx: any): number

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(idx: Vec): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(idx: Vec): number

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(idx: Vec): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(idx: Vec): number

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(i0: number, i1: number, i2: number): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(i0: number, i1: number, i2: number): number

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(i0: number, i1: number, i2: number): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(i0: number, i1: number, i2: number): number

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(i0?: number): _Tp

/**
 * Returns a pointer to the specified matrix row.
 *
 * The methods return `uchar*` or typed pointer to the specified matrix row. See the sample in Mat::isContinuous to know how to use these methods.
 *
 * @param i0 A 0-based row index.
 */
  public ptr(i0?: number): number

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(i0?: number): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public ptr(i0?: number): number

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param row Index along the dimension 0
 * @param col Index along the dimension 1
 */
  public ptr(row: number, col: number): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param row Index along the dimension 0
 * @param col Index along the dimension 1
 */
  public ptr(row: number, col: number): number

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param row Index along the dimension 0
 * @param col Index along the dimension 1
 */
  public ptr(row: number, col: number): _Tp

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param row Index along the dimension 0
 * @param col Index along the dimension 1
 */
  public ptr(row: number, col: number): number

/**
 * Adds elements to the bottom of the matrix.
 *
 * The methods add one or more elements to the bottom of the matrix. They emulate the corresponding method of the STL vector class. When elem is Mat , its type and the number of columns must be the same as in the container matrix.
 *
 * @param elem Added element(s).
 */
  public push_back(elem: _Tp): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param m Added line(s).
 */
  public push_back(m: Mat): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param elem Added element(s).
 */
  public push_back(elem: Mat_): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param elem Added element(s).
 */
  public push_back(elem: _Tp): void

/**
 * internal function
 */
  public push_back_(elem: any): void

/**
 * Same as begin() but for inverse traversal.
 */
  public rbegin(): MatIterator_

  public rbegin(): MatConstIterator_

/**
 * Reset the type of matrix.
 *
 * The methods reset the data type of matrix. If the new type and the old type of the matrix have the same element size, the current buffer can be reused. The method needs to consider whether the current mat is a submatrix or has any references.
 *
 * @param type New data type.
 */
  public reinterpret(type: number): Mat

/**
 * Decrements the reference counter and deallocates the matrix if needed.
 *
 * The method decrements the reference counter associated with the matrix data. When the reference counter reaches 0, the matrix data is deallocated and the data and the reference counter pointers are set to NULL's. If the matrix header points to an external data set (see Mat::Mat ), the reference counter is NULL, and the method has no effect in this case.
 *
 *
 * This method can be called manually to force the matrix data deallocation. But since this method is automatically called in the destructor, or by any other method that changes the data pointer, it is usually not needed. The reference counter decrement and check for 0 is an atomic operation on the platforms that support it. Thus, it is safe to operate on the same matrices asynchronously in different threads.
 */
  public release(): void

/**
 * Same as end() but for inverse traversal.
 */
  public rend(): MatIterator_

  public rend(): MatConstIterator_

/**
 * Reserves space for the certain number of rows.
 *
 * The method reserves space for sz rows. If the matrix already has enough space to store sz rows, nothing happens. If the matrix is reallocated, the first Mat::rows rows are preserved. The method emulates the corresponding method of the STL vector class.
 *
 * @param sz Number of rows.
 */
  public reserve(sz: number): void

/**
 * Reserves space for the certain number of bytes.
 *
 * The method reserves space for sz bytes. If the matrix already has enough space to store sz bytes, nothing happens. If matrix has to be reallocated its previous content could be lost.
 *
 * @param sz Number of bytes.
 */
  public reserveBuffer(sz: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param cn New number of channels. If the parameter is 0, the number of channels remains the same.
 * @param newshape New shape in the form of MatShape.
 */
  public reshape(cn: number, newshape: MatShape): Mat

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param cn New number of channels. If the parameter is 0, the number of channels remains the same.
 * @param newshape Vector with new matrix size by all dimensions. If some sizes are zero, the original sizes in those dimensions are presumed.
 */
  public reshape(cn: number, newshape: any): Mat

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param cn New number of channels. If the parameter is 0, the number of channels remains the same.
 * @param newndims New number of dimensions.
 * @param newsz Array with new matrix size by all dimensions. If some sizes are zero, the original sizes in those dimensions are presumed.
 */
  public reshape(cn: number, newndims: number, newsz: any): Mat

/**
 * Changes the shape and/or the number of channels of a 2D matrix without copying the data.
 *
 * The method makes a new matrix header for *this elements. The new matrix may have a different size and/or different number of channels. Any combination is possible if:
 * - No extra elements are included into the new matrix and no elements are excluded. Consequently, the product rows*cols*channels() must stay the same after the transformation.
 * - No data is copied. That is, this is an O(1) operation. Consequently, if you change the number of rows, or the operation changes the indices of elements row in some other way, the matrix must be continuous. See Mat::isContinuous .
 *
 *
 * For example, if there is a set of 3D points stored as an STL vector, and you want to represent the points as a 3xN matrix, do the following: std::vector<Point3f>vec;
 * ...
 * MatpointMat=Mat(vec).//convertvectortoMat,O(1)operation
 * reshape(1).//makeNx31-channelmatrixoutofNx13-channel.
 * //Also,anO(1)operation
 * t();//finally,transposetheNx3matrix.
 * //Thisinvolvescopyingalltheelements
 *  3-channel 2x2 matrix reshaped to 1-channel 4x3 matrix, each column has values from one of original channels: Matm(Size(2,2),CV_8UC3,Scalar(1,2,3));
 * vector<int>new_shape{4,3};
 * m=m.reshape(1,new_shape);
 *  or: Matm(Size(2,2),CV_8UC3,Scalar(1,2,3));
 * constintnew_shape[]={4,3};
 * m=m.reshape(1,2,new_shape);
 *
 * @param cn New number of channels. If the parameter is 0, the number of channels remains the same.
 * @param rows New number of rows. If the parameter is 0, the number of rows remains the same.
 */
  public reshape(cn: number, rows?: number): Mat

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param cn New number of channels. If the parameter is 0, the number of channels remains the same.
 * @param newshape New shape in the form of initializer list.
 */
  public reshape(cn: number, newshape: any): Mat

/**
 * Changes the number of matrix rows.
 *
 * The methods change the number of matrix rows. If the matrix is reallocated, the first min(Mat::rows, sz) rows are preserved. The methods emulate the corresponding methods of the STL vector class.
 *
 * @param sz New number of rows.
 */
  public resize(sz: number): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param sz New number of rows.
 * @param s Value assigned to the newly added elements.
 */
  public resize(sz: number, s: Scalar): void

/**
 * Creates a matrix header for the specified matrix row.
 *
 * The method makes a new header for the specified matrix row and returns it. This is an O(1) operation, regardless of the matrix size. The underlying data of the new matrix is shared with the original matrix. Here is the example of one of the classical basic matrix processing operations, axpy, used by LU and many other algorithms: inlinevoidmatrix_axpy(Mat&A,inti,intj,doublealpha)
 * {
 * A.row(i)+=A.row(j)*alpha;
 * }
 *  @note In the current implementation, the following code does not work as expected: MatA;
 * ...
 * A.row(i)=A.row(j);//willnotwork
 *  This happens because A.row(i) forms a temporary header that is further assigned to another header. Remember that each of these operations is O(1), that is, no data is copied. Thus, the above assignment is not true if you may have expected the j-th row to be copied to the i-th row. To achieve that, you should either turn this simple assignment into an expression or use the Mat::copyTo method: MatA;
 * ...
 * //works,butlooksabitobscure.
 * A.row(i)=A.row(j)+0;
 * //thisisabitlonger,buttherecommendedmethod.
 * A.row(j).copyTo(A.row(i));
 *
 * @param y A 0-based row index.
 */
  public row(y: number): Mat

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param r Range structure containing both the start and the end indices.
 */
  public rowRange(r: Range): Mat

/**
 * Creates a matrix header for the specified row span.
 *
 * The method makes a new header for the specified row span of the matrix. Similarly to Mat::row and Mat::col , this is an O(1) operation.
 *
 * @param startrow An inclusive 0-based start index of the row span.
 * @param endrow An exclusive 0-based ending index of the row span.
 */
  public rowRange(startrow: number, endrow: number): Mat

/**
 * Sets all or some of the array elements to the specified value.
 *
 * This is an advanced variant of the Mat::operator=(const Scalar& s) operator.
 *
 * @param value Assigned scalar converted to the actual array type.
 * @param mask Operation mask of the same size as *this. Its non-zero elements indicate which matrix elements need to be copied. The mask has to be of type CV_8U, CV_8S or CV_Bool and can have 1 or multiple channels.
 */
  public setTo(value: InputArray, mask?: InputArray): Mat

/**
 * Sets all the array elements to 0.
 */
  public setZero(): Mat

/**
 * Returns the shape.
 */
  public shape(): MatShape

/**
 * Returns a normalized step.
 *
 * The method returns a matrix step divided by Mat::elemSize1() . It can be useful to quickly access an arbitrary matrix element.
 */
  public step1(i?: number): number

/**
 * Transposes a matrix.
 *
 * The method performs matrix transposition by means of matrix expressions. It does not perform the actual transposition but returns a temporary matrix transposition object that can be further used as a part of more complex matrix expressions or can be assigned to a matrix: MatA1=A+Mat::eye(A.size(),A.type())*lambda;
 * MatC=A1.t()*A1;//compute(A+lambda*I)^t*(A+lamda*I)
 */
  public t(): MatExpr

/**
 * Returns the total number of array elements.
 *
 * The method returns the number of array elements (a number of pixels if the array represents an image).
 */
  public total(): number

/**
 * Returns the total number of array elements.
 *
 * The method returns the number of elements within a certain sub-array slice with startDim <= dim < endDim
 */
  public total(startDim: number, endDim?: number): number

/**
 * Returns the type of a matrix element.
 *
 * The method returns a matrix element type. This is an identifier compatible with the CvMat type system, like CV_16SC3 or 16-bit signed 3-channel array, and so on.
 */
  public type(): number

/**
 * internal use method: updates the continuity flag
 */
  public updateContinuityFlag(): void

/**
 * creates a diagonal matrix
 *
 * The method creates a square diagonal matrix from specified main diagonal.
 *
 * @param d One-dimensional matrix that represents the main diagonal.
 */
  public static diag(d: Mat): CV_NODISCARD_STD

/**
 * Returns an identity matrix of the specified size and type.
 *
 * The method returns a Matlab-style identity matrix initializer, similarly to Mat::zeros. Similarly to Mat::ones, you can use a scale operation to create a scaled identity matrix efficiently: //makea4x4diagonalmatrixwith0.1'sonthediagonal.
 * MatA=Mat::eye(4,4,CV_32F)*0.1;
 *  @note In case of multi-channels type, identity matrix will be initialized only for the first channel, the others will be set to 0's
 *
 * @param rows Number of rows.
 * @param cols Number of columns.
 * @param type Created matrix type.
 */
  public static eye(rows: number, cols: number, type: number): CV_NODISCARD_STD

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param size Alternative matrix size specification as Size(cols, rows) .
 * @param type Created matrix type.
 */
  public static eye(size: Size, type: number): CV_NODISCARD_STD

  public static getDefaultAllocator(): MatAllocator

/**
 * and the standard allocator
 */
  public static getStdAllocator(): MatAllocator

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param shape Array shape.
 * @param type Created matrix type.
 */
  public static ones(shape: MatShape, type: number): CV_NODISCARD_STD

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param ndims Array dimensionality.
 * @param sz Array of integers specifying the array shape.
 * @param type Created matrix type.
 */
  public static ones(ndims: number, sz: any, type: number): CV_NODISCARD_STD

/**
 * Returns an array of all 1's of the specified size and type.
 *
 * The method returns a Matlab-style 1's array initializer, similarly to Mat::zeros. Note that using this method you can initialize an array with an arbitrary value, using the following Matlab idiom: MatA=Mat::ones(100,100,CV_8U)*3;//make100x100matrixfilledwith3.
 *  The above operation does not form a 100x100 matrix of 1's and then multiply it by 3. Instead, it just remembers the scale factor (3 in this case) and use it when actually invoking the matrix initializer. @note In case of multi-channels type, only the first channel will be initialized with 1's, the others will be set to 0's.
 *
 * @param rows Number of rows.
 * @param cols Number of columns.
 * @param type Created matrix type.
 */
  public static ones(rows: number, cols: number, type: number): CV_NODISCARD_STD

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param size Alternative to the matrix size specification Size(cols, rows) .
 * @param type Created matrix type.
 */
  public static ones(size: Size, type: number): CV_NODISCARD_STD

  public static setDefaultAllocator(allocator: MatAllocator): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param shape Array shape.
 * @param type Created matrix type.
 */
  public static zeros(shape: MatShape, type: number): CV_NODISCARD_STD

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param ndims Array dimensionality.
 * @param sz Array of integers specifying the array shape.
 * @param type Created matrix type.
 */
  public static zeros(ndims: number, sz: any, type: number): CV_NODISCARD_STD

/**
 * Returns a zero array of the specified size and type.
 *
 * The method returns a Matlab-style zero array initializer. It can be used to quickly form a constant array as a function parameter, part of a matrix expression, or as a matrix initializer: MatA;
 * A=Mat::zeros(3,3,CV_32F);
 *  In the example above, a new matrix is allocated only if A is not a 3x3 floating-point matrix. Otherwise, the existing matrix A is filled with zeros.
 *
 * @param rows Number of rows.
 * @param cols Number of columns.
 * @param type Created matrix type.
 */
  public static zeros(rows: number, cols: number, type: number): CV_NODISCARD_STD

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param size Alternative to the matrix size specification Size(cols, rows) .
 * @param type Created matrix type.
 */
  public static zeros(size: Size, type: number): CV_NODISCARD_STD
}

export declare const Mat_MAGIC_MASK: number

export declare const Mat_TYPE_MASK: number

export declare const Mat_DEPTH_MASK: number

export declare const Mat_MAGIC_VAL: number

export declare const Mat_AUTO_STEP: number

export declare const Mat_CONTINUOUS_FLAG: number

export declare const Mat_SUBMATRIX_FLAG: number
