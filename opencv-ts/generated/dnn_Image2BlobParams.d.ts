// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { Vector } from '../hacks/runtime';
import type { Rect, Scalar, Size } from '../hacks/scalars';
import type { DataLayout, ImagePaddingMode } from './_unresolved';

/**
 * Processing params of image to blob.
 *
 * It includes all possible image processing operations and corresponding parameters.
 *
 *
 * @see blobFromImageWithParams
 *
 * @note The order and usage of `scalefactor` and `mean` are (input - mean) * scalefactor. The order and usage of `scalefactor`, `size`, `mean`, `swapRB`, and `ddepth` are consistent with the function of blobFromImage.
 */
export declare class dnn_Image2BlobParams {
/**
 * Value used in padding mode for padding.
 */
  public borderValue: Scalar

/**
 * Order of output dimensions. Choose DNN_LAYOUT_NCHW or DNN_LAYOUT_NHWC.
 */
  public datalayout: DataLayout

/**
 * Depth of output blob. Choose CV_32F or CV_8U.
 */
  public ddepth: number

/**
 * Scalar with mean values which are subtracted from channels.
 */
  public mean: Scalar

/**
 * Image padding mode.
 *
 * @see ImagePaddingMode.
 */
  public paddingmode: any

/**
 * scalefactor multiplier for input image values.
 */
  public scalefactor: Scalar

/**
 * Spatial size for output image.
 */
  public size: Size

/**
 * Flag which indicates that swap first and last channels.
 */
  public swapRB: boolean

  public constructor()

  public constructor(scalefactor: Scalar, size?: Size, mean?: Scalar, swapRB?: boolean, ddepth?: number, datalayout?: DataLayout, mode?: ImagePaddingMode, borderValue?: Scalar)

/**
 * Get rectangle coordinates in original image system from rectangle in blob coordinates.
 *
 * @param rBlob rect in blob coordinates.
 * @param rImg result rect in image coordinates.
 * @param size original input image size.
 */
  public blobRectsToImageRects(rBlob: Rect, rImg: Vector<any>, size: Size): void

/**
 * Get rectangle coordinates in original image system from rectangle in blob coordinates.
 *
 * @return rectangle in original image coordinates.
 *
 * @param rBlob rect in blob coordinates.
 * @param size original input image size.
 */
  public blobRectToImageRect(rBlob: Rect, size: Size): Rect
}
