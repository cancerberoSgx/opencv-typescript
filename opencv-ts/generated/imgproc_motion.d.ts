// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray, InputOutputArray, OutputArray } from '../hacks/mat';
import type { Point2d, Size } from '../hacks/scalars';
import type { L2size } from './_unresolved';

/* @defgroup Motion Analysis and Object Tracking */

/**
 * Adds an image to the accumulator image.
 *
 * The function adds src or some of its elements to dst :
 *
 *
 *
 *
 *
 * The function supports multi-channel images. Each channel is processed independently.
 *
 *
 * The function cv::accumulate can be used, for example, to collect statistics of a scene background viewed by a still camera and for the further foreground-background segmentation.
 *
 *
 * @see accumulateSquare, accumulateProduct, accumulateWeighted
 *
 * @param src Input image of type CV_8UC(n), CV_16UC(n), CV_32FC(n) or CV_64FC(n), where n is a positive integer.
 * @param dst Accumulator image with the same number of channels as input image, and a depth of CV_32F or CV_64F.
 * @param mask Optional operation mask.
 */
export declare function accumulate(src: InputArray, dst: InputOutputArray, mask?: InputArray): void

/**
 * Adds the per-element product of two input images to the accumulator image.
 *
 * The function adds the product of two images or their selected regions to the accumulator dst :
 *
 *
 *
 *
 *
 * The function supports multi-channel images. Each channel is processed independently.
 *
 *
 * @see accumulate, accumulateSquare, accumulateWeighted
 *
 * @param src1 First input image, 1- or 3-channel, 8-bit or 32-bit floating point.
 * @param src2 Second input image of the same type and the same size as src1 .
 * @param dst Accumulator image with the same number of channels as input images, 32-bit or 64-bit floating-point.
 * @param mask Optional operation mask.
 */
export declare function accumulateProduct(src1: InputArray, src2: InputArray, dst: InputOutputArray, mask?: InputArray): void

/**
 * Adds the square of a source image to the accumulator image.
 *
 * The function adds the input image src or its selected region, raised to a power of 2, to the accumulator dst :
 *
 *
 *
 *
 *
 * The function supports multi-channel images. Each channel is processed independently.
 *
 *
 * @see accumulateSquare, accumulateProduct, accumulateWeighted
 *
 * @param src Input image as 1- or 3-channel, 8-bit or 32-bit floating point.
 * @param dst Accumulator image with the same number of channels as input image, 32-bit or 64-bit floating-point.
 * @param mask Optional operation mask.
 */
export declare function accumulateSquare(src: InputArray, dst: InputOutputArray, mask?: InputArray): void

/**
 * Updates a running average.
 *
 * The function calculates the weighted sum of the input image src and the accumulator dst so that dst becomes a running average of a frame sequence:
 *
 *
 *
 *
 *
 * That is, alpha regulates the update speed (how fast the accumulator "forgets" about earlier images). The function supports multi-channel images. Each channel is processed independently.
 *
 *
 * @see accumulate, accumulateSquare, accumulateProduct
 *
 * @param src Input image as 1- or 3-channel, 8-bit or 32-bit floating point.
 * @param dst Accumulator image with the same number of channels as input image, 32-bit or 64-bit floating-point.
 * @param alpha Weight of the input image.
 * @param mask Optional operation mask.
 */
export declare function accumulateWeighted(src: InputArray, dst: InputOutputArray, alpha: number, mask?: InputArray): void

/**
 * This function computes a Hanning window coefficients in two dimensions.
 *
 * See (https://en.wikipedia.org/wiki/Hann_function (https://en.wikipedia.org/wiki/Hann_function)) and (https://en.wikipedia.org/wiki/Window_function (https://en.wikipedia.org/wiki/Window_function)) for more information.
 *
 *
 * An example is shown below: //createhanningwindowofsize100x100andtypeCV_32F
 * Mathann;
 * createHanningWindow(hann,Size(100,100),CV_32F);
 *
 * @param dst Destination array to place Hann coefficients in
 * @param winSize The window size specifications (both width and height must be > 1)
 * @param type Created array type
 */
export declare function createHanningWindow(dst: OutputArray, winSize: Size, type: number): void

/**
 * Detects translational shifts between two images.
 *
 * This function extends the standard phaseCorrelate method by improving sub-pixel accuracy through iterative shift refinement in the phase-correlation space, as described in hrazdira2020iterative.
 *
 *
 * @return detected sub-pixel shift between the two arrays.
 *
 * @see phaseCorrelate, dft, idft, createHanningWindow
 *
 * @param src1 Source floating point array (CV_32FC1 or CV_64FC1)
 * @param src2 Source floating point array (CV_32FC1 or CV_64FC1)
 * @param L2size The size of the correlation neighborhood used by the iterative shift refinement algorithm.
 * @param maxIters The maximum number of iterations the iterative refinement algorithm will run.
 */
export declare function phaseCorrelateIterative(src1: InputArray, src2: InputArray, L2size?: number, maxIters?: number): Point2d
