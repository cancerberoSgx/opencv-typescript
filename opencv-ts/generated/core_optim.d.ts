// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray, OutputArray } from '../hacks/mat';
import type { Constr, Func } from './_unresolved';

/**
 * The algorithms in this section minimize or maximize function value within specified constraints or without any constraints.
 */

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
export declare function solveLP(Func: InputArray, Constr: InputArray, z: OutputArray): number

/**
 * Solve given (non-integer) linear programming problem using the Simplex Algorithm (Simplex Method).
 *
 * What we mean here by "linear programming problem" (or LP problem, for short) can be formulated as:
 *
 *
 *
 *
 *
 * Where  is fixed `1`-by-`n` row-vector,  is fixed `m`-by-`n` matrix,  is fixed `m`-by-`1` column vector and  is an arbitrary `n`-by-`1` column vector, which satisfies the constraints.
 *
 *
 * Simplex algorithm is one of many algorithms that are designed to handle this sort of problems efficiently. Although it is not optimal in theoretical sense (there exist algorithms that can solve any problem written as above in polynomial time, while simplex method degenerates to exponential time for some special cases), it is well-studied, easy to implement and is shown to work well for real-life purposes.
 *
 *
 * The particular implementation is taken almost verbatim from **Introduction to Algorithms, third edition** by T. H. Cormen, C. E. Leiserson, R. L. Rivest and Clifford Stein. In particular, the Bland's rule http://en.wikipedia.org/wiki/Bland%27s_rule (http://en.wikipedia.org/wiki/Bland%27s_rule) is used to prevent cycling.
 *
 *
 * @return One of cv::SolveLPResult
 *
 * @param Func This row-vector corresponds to  in the LP problem formulation (see above). It should contain 32- or 64-bit floating point numbers. As a convenience, column-vector may be also submitted, in the latter case it is understood to correspond to .
 * @param Constr `m`-by-`n+1` matrix, whose rightmost column corresponds to  in formulation above and the remaining to . It should contain 32- or 64-bit floating point numbers.
 * @param z The solution will be returned here as a column-vector - it corresponds to  in the formulation above. It will contain 64-bit floating point numbers.
 * @param constr_eps allowed numeric disparity for constraints
 */
export declare function solveLP(Func: InputArray, Constr: InputArray, z: OutputArray, constr_eps: number): number

/**
 * problem is feasible, but solver lost solution due to floating-point arithmetic errors
 */
export declare const SOLVELP_LOST: number

/**
 * problem is unbounded (target function can achieve arbitrary high values)
 */
export declare const SOLVELP_UNBOUNDED: number

/**
 * problem is unfeasible (there are no points that satisfy all the constraints imposed)
 */
export declare const SOLVELP_UNFEASIBLE: number

/**
 * there is only one maximum for target function
 */
export declare const SOLVELP_SINGLE: number

/**
 * there are multiple maxima for target function - the arbitrary one is returned
 */
export declare const SOLVELP_MULTI: number
