// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray } from '../hacks/mat';
import type { Vector } from '../hacks/runtime';
import type { dnn_Model } from './dnn_Model';

/**
 * Base class for text detection networks.
 */
export declare class dnn_TextDetectionModel extends dnn_Model {
/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public detect(frame: InputArray, detections: Vector<any>): void

/**
 * Performs detection.
 *
 * Given the input `frame`, prepare network input, run network inference, post-process network output and return result detections.
 *
 *
 * Each result is quadrangle's 4 points in this order:
 * - bottom-left
 * - top-left
 * - top-right
 * - bottom-right
 *
 *
 * Use cv::getPerspectiveTransform function to retrieve image region without perspective transformations.
 *
 *
 * @note If DL model doesn't support that kind of output then result may be derived from detectTextRectangles() output.
 *
 * @param frame The input image
 * @param detections array with detections' quadrangles (4 points per result)
 * @param confidences array with detection confidences
 */
  public detect(frame: InputArray, detections: Vector<any>, confidences: Vector<any>): void

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 */
  public detectTextRectangles(frame: InputArray, detections: Vector<any>): void

/**
 * Performs detection.
 *
 * Given the input `frame`, prepare network input, run network inference, post-process network output and return result detections.
 *
 *
 * Each result is rotated rectangle.
 *
 *
 * @note Result may be inaccurate in case of strong perspective transformations.
 *
 * @param frame the input image
 * @param detections array with detections' RotationRect results
 * @param confidences array with detection confidences
 */
  public detectTextRectangles(frame: InputArray, detections: Vector<any>, confidences: Vector<any>): void
}
