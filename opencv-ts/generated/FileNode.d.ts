// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { Vector } from '../hacks/runtime';
import type { FileStorage } from './FileStorage';
import type { Mat } from './Mat';
import type { FileNodeIterator } from './_unresolved';

/**
 * File Storage Node class.
 *
 * The node is used to store each and every element of the file storage opened for reading. When XML/YAML file is read, it is first parsed and stored in the memory as a hierarchical collection of nodes. Each node can be a "leaf" that is contain a single number or a string, or be a collection of other nodes. There can be named collections (mappings) where each element has a name and it is accessed by a name, and ordered collections (sequences) where elements do not have names but rather accessed by index. Type of the file node can be determined using FileNode::type method.
 *
 *
 * Note that file nodes are only used for navigating file storages opened for reading. When a file storage is opened for writing, no data is stored in memory after it is written.
 */
export declare class FileNode {
  public blockIdx: number

  public fs: any

  public ofs: number

/**
 * The constructors.
 *
 * These constructors are used to create a default file node, construct it from obsolete structures or from the another file node.
 */
  public constructor()

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 *
 * @param node File node to be used as initialization for the created file node.
 */
  public constructor(node: FileNode)

/**
 * This is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.
 * Deprecated
 *
 * @param fs Pointer to the file storage structure.
 * @param blockIdx Index of the memory block where the file node is stored
 * @param ofs Offset in bytes from the beginning of the serialized storage
 */
  public constructor(fs: FileStorage, blockIdx: number, ofs: number)

  public constructor(fs: any, blockIdx: number, ofs: number)

/**
 * returns iterator pointing to the first node element
 */
  public begin(): FileNodeIterator

/**
 * returns true if the node is empty
 */
  public empty(): boolean

/**
 * returns iterator pointing to the element following the last node element
 */
  public end(): FileNodeIterator

/**
 * returns true if the node is an integer
 */
  public isInt(): boolean

/**
 * returns true if the node is a mapping
 */
  public isMap(): boolean

/**
 * returns true if the node has a name
 */
  public isNamed(): boolean

/**
 * returns true if the node is a "none" object
 */
  public isNone(): boolean

/**
 * returns true if the node is a floating-point number
 */
  public isReal(): boolean

/**
 * returns true if the node is a sequence
 */
  public isSeq(): boolean

/**
 * returns true if the node is a text string
 */
  public isString(): boolean

/**
 * Returns keys of a mapping node.
 *
 * @return Keys of a mapping node.
 */
  public keys(): Vector<any>

/**
 * Simplified reading API to use with bindings.
 */
  public mat(): Mat

/**
 * returns the node name or an empty string if the node is nameless
 */
  public name(): any

  public ptr(): number

  public ptr(): number

/**
 * returns raw size of the FileNode in bytes
 */
  public rawSize(): number

/**
 * Reads node elements to the buffer with the specified format.
 *
 * Usually it is more convenient to use operator `>>` instead of this method.
 *
 * @param fmt Specification of each array element. See format specification
 * @param vec Pointer to the destination array.
 * @param len Number of bytes to read (buffer size limit). If it is greater than number of remaining elements then all of them will be read.
 */
  public readRaw(fmt: String, vec: any, len: number): void

/**
 * Simplified reading API to use with bindings.
 */
  public real(): number

/**
 * Internal method used when reading FileStorage. Sets the type (int, real or string) and value of the previously created node.
 */
  public setValue(type: number, value: any, len?: number): void

/**
 * returns the number of elements in the node, if it is a sequence or mapping, or 1 otherwise.
 */
  public size(): number

/**
 * Simplified reading API to use with bindings.
 */
  public string(): any

/**
 * Returns type of the node.
 *
 * @return Type of the node. See FileNode::Type
 */
  public type(): number

  public static isCollection(flags: number): boolean

  public static isEmptyCollection(flags: number): boolean

  public static isFlow(flags: number): boolean

  public static isMap(flags: number): boolean

  public static isSeq(flags: number): boolean
}

/**
 * empty node
 */
export declare const FileNode_NONE: number

/**
 * an integer
 */
export declare const FileNode_INT: number

/**
 * floating-point number
 */
export declare const FileNode_REAL: number

/**
 * synonym or REAL
 */
export declare const FileNode_FLOAT: number

/**
 * text string in UTF-8 encoding
 */
export declare const FileNode_STR: number

/**
 * synonym for STR
 */
export declare const FileNode_STRING: number

/**
 * sequence
 */
export declare const FileNode_SEQ: number

/**
 * mapping
 */
export declare const FileNode_MAP: number

export declare const FileNode_TYPE_MASK: number

/**
 * compact representation of a sequence or mapping. Used only by YAML writer
 */
export declare const FileNode_FLOW: number

/**
 * UNIFORM is used only when reading FileStorage; FLOW is used only when writing. So they share the same bit.
 *
 * if set, means that all the collection elements are numbers of the same type (real's or int's).
 */
export declare const FileNode_UNIFORM: number

/**
 * empty structure (sequence or mapping)
 */
export declare const FileNode_EMPTY: number

/**
 * the node has a name (i.e. it is element of a mapping).
 */
export declare const FileNode_NAMED: number
