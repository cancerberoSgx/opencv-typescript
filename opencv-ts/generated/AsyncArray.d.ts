// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { OutputArray } from '../hacks/mat';

/**
 * Returns result of asynchronous operations.
 *
 * Object has attached asynchronous state. Assignment operator doesn't clone asynchronous state (it is shared between all instances).
 *
 *
 * Result can be fetched via get() method only once.
 */
export declare class AsyncArray {
  public constructor()

  public constructor(o: AsyncArray)

  public constructor(o: AsyncArray)

  public _getImpl(): any

/**
 * Fetch the result.
 * Waits for result until container has valid result. Throws exception if exception was stored as a result.
 *
 *
 * Throws exception on invalid container state.
 *
 *
 * @note Result or stored exception can be fetched only once.
 *
 * @param dst destination array
 */
  public get(dst: OutputArray): void

  public get(dst: OutputArray, timeoutNs: number): boolean

/**
 * Retrieving the result with timeout
 * @return true if result is ready, false if the timeout has expired
 *
 * @note Result or stored exception can be fetched only once.
 *
 * @param dst destination array
 * @param timeoutNs timeout in nanoseconds, -1 for infinite wait
 */
  public get(dst: OutputArray, timeoutNs: number): boolean

  public release(): void

  public valid(): boolean

  public wait_for(timeoutNs: number): boolean

  public wait_for(timeoutNs: number): boolean
}
