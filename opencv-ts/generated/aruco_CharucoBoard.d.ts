// AUTO-GENERATED by opencv-types-generator - do not edit by hand.

import type { InputArray } from '../hacks/mat';
import type { Vector } from '../hacks/runtime';
import type { Size } from '../hacks/scalars';
import type { CharucoBoard, Dictionary } from './_unresolved';
import type { aruco_Board } from './aruco_Board';

/**
 * ChArUco board is a planar chessboard where the markers are placed inside the white squares of a chessboard.
 *
 * The benefits of ChArUco boards is that they provide both, ArUco markers versatility and chessboard corner precision, which is important for calibration and pose estimation. The board image can be drawn using generateImage() method.
 */
export declare class aruco_CharucoBoard extends aruco_Board {
  public CharucoBoard(): any

/**
 * CharucoBoard constructor.
 *
 * @param size number of chessboard squares in x and y directions
 * @param squareLength squareLength chessboard square side length (normally in meters)
 * @param markerLength marker side length (same unit than squareLength)
 * @param dictionary dictionary of markers indicating the type of markers
 * @param ids array of id used markers The first markers in the dictionary are used to fill the white chessboard squares.
 */
  public CharucoBoard(size: Size, squareLength: number, markerLength: number, dictionary: Dictionary, ids?: InputArray): any

/**
 * check whether the ChArUco markers are collinear
 *
 * @return bool value, 1 (true) if detected corners form a line, 0 (false) if they do not. solvePnP, calibration functions will fail if the corners are collinear (true).
 *
 * The number of ids in charucoIDs should be <= the number of chessboard corners in the board. This functions checks whether the charuco corners are on a straight line (returns true, if so), or not (false). Axis parallel, as well as diagonal and other straight lines detected. Degenerate cases: for number of charucoIDs <= 2,the function returns true.
 *
 * @param charucoIds list of identifiers for each corner in charucoCorners per frame.
 */
  public checkCharucoCornersCollinear(charucoIds: InputArray): boolean

/**
 * get CharucoBoard::chessboardCorners
 */
  public getChessboardCorners(): Vector<any>

  public getChessboardSize(): Size

  public getLegacyPattern(): boolean

  public getMarkerLength(): number

/**
 * get CharucoBoard::nearestMarkerCorners, for each charuco corner, nearest marker corner id of each marker
 */
  public getNearestMarkerCorners(): Vector<any>

/**
 * get CharucoBoard::nearestMarkerIdx, for each charuco corner, nearest marker index in ids array
 */
  public getNearestMarkerIdx(): Vector<any>

  public getSquareLength(): number

/**
 * set legacy chessboard pattern.
 *
 * Legacy setting creates chessboard patterns starting with a white box in the upper left corner if there is an even row count of chessboard boxes, otherwise it starts with a black box. This setting ensures compatibility to patterns created with OpenCV versions prior OpenCV 4.6.0. See https://github.com/opencv/opencv/issues/23152 (https://github.com/opencv/opencv/issues/23152).
 *
 *
 * Default value: false.
 */
  public setLegacyPattern(legacyPattern: boolean): void
}
