node_modules
dist
dist-ssr
*.local

# Copied in by `npm run setup:opencv` from a local OpenCV build - large generated binary,
# and its version must match whatever `opencv-ts` was generated from (see README.md).
public/opencv.js
